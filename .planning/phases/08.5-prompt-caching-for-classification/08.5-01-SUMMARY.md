---
phase: 08.5-prompt-caching-for-classification
plan: 01
subsystem: classification
tags: [anthropic, prompt-caching, ai-sdk, cost-reduction, ops-03]

requires:
  - phase: 03-classification-engine
    provides: "Haiku classifier path (getAiResponseSingleRule) — the high-volume target for caching"
provides:
  - "Provider-gated Anthropic prompt caching on the classifier system prompt (both single-rule and multi-rule paths)"
  - "isAnthropicProvider + buildClassifierRequest helpers in ai-choose-rule.ts (reusable pattern)"
  - "NOTES.md documents the cut point so Phase 9 extraction prompt can mirror without re-discovery"
affects: [phase-09-reconciliation, future-extraction-prompts]

tech-stack:
  added: []
  patterns:
    - "AI SDK messages array with providerOptions.anthropic.cacheControl on system text block — gated on provider === 'anthropic'"
    - "Non-Anthropic providers retain { system, prompt } shape — no regression"

key-files:
  created:
    - .planning/phases/08.5-prompt-caching-for-classification/08.5-01-SUMMARY.md
  modified:
    - apps/web/utils/ai/choose-rule/ai-choose-rule.ts
    - apps/web/utils/ai/choose-rule/ai-choose-rule.test.ts
    - apps/web/utils/ai/choose-rule/NOTES.md

key-decisions:
  - "Single ephemeral cache breakpoint on the full system block — multi-breakpoint deferred to Phase 9+ if measurements justify"
  - "Provider gate uses string equality on Provider.ANTHROPIC; anthropic-vertex / bedrock take the non-Anthropic branch (documented in NOTES.md — extend helper if those become primary paths)"
  - "applyPromptHardeningToSystem is bypassed on the Anthropic path because system is no longer passed as a string — accepted per CONTEXT D-02 since classifier output is a constrained schema"
  - "No telemetry plumbing — Anthropic Console (https://console.anthropic.com/settings/usage) is the source of truth for cache_read_input_tokens"

patterns-established:
  - "Provider-gated request-shape switch — pattern for any future cached prompts (Phase 9 extraction will mirror)"

requirements-completed:
  - OPS-03

metrics:
  duration: ~25min
  completed: 2026-05-22
  tasks: 4
  files_modified: 3
---

# Phase 08.5 Plan 01: Enable Anthropic Prompt Caching for Classification — Summary

**Anthropic prompt caching enabled on both classifier paths — repeat classifications now pay ~10% of input-token price on the cached system prefix; non-Anthropic providers unchanged.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-05-22
- **Tasks:** 4 (3 auto + 1 lint pass)
- **Files modified:** 3
- **Commits:** 4 (T-01 feat, T-02 test, T-03 docs, T-04 style)

## Accomplishments

- Added `isAnthropicProvider(provider)` + `buildClassifierRequest({ modelOptions, system, prompt, schema })` helpers in `ai-choose-rule.ts`
- Refactored `getAiResponseSingleRule` (Tier 2 Haiku) and `getAiResponseMultiRule` (default-model path) to route their `generateObject` call through `buildClassifierRequest`
- Anthropic path: `messages` array with `providerOptions.anthropic.cacheControl = { type: 'ephemeral' }` on the system text block; no top-level `system`/`prompt`
- Non-Anthropic path (openai/openrouter/google/groq/etc.): retains existing `{ system, prompt }` shape — no regression
- 4 new unit tests in `ai-choose-rule.test.ts` assert request shape for both branches (Anthropic single-rule, non-Anthropic single-rule, Anthropic multi-rule, non-Anthropic multi-rule). All 7 tests pass (3 existing CLASS-02 + 4 new OPS-03).
- Appended "Prompt Caching (Anthropic only, since Phase 8.5)" section to `NOTES.md` documenting cut point, AI SDK pattern, provider gate, hardening caveat, and verification approach for Phase 9 to mirror

## Verification

- **Unit tests:** `pnpm vitest run utils/ai/choose-rule/ai-choose-rule.test.ts` → 7 passed (3 CLASS-02 + 4 OPS-03)
- **Grep gates:** Task 1 and Task 3 verify commands pass — code/docs contain required tokens (`isAnthropicProvider`, `cacheControl: { type: 'ephemeral' }`, `Provider.ANTHROPIC`, "Prompt Caching" section, etc.)
- **Lint:** Biome targeted check on the 3 modified files is clean post-format. Repo-wide `pnpm lint` has 1963 pre-existing errors outside Phase 8.5's files (not blocking per plan acceptance criteria)
- **Post-deploy (PR description, not blocking merge):** Re-check https://console.anthropic.com/settings/usage 24h after deploy and confirm `cache_read_input_tokens > 0`. Result to be recorded in CONTEXT decision log per D-05.

## Deviations from Plan

None — plan executed exactly as written. Task 1 source already existed at agent start (committed in eb755a3b0 from a prior session); test file changes (Task 2) were uncommitted and exactly matched the plan specification; Task 3 (NOTES.md) and Task 4 (lint) executed fresh.

One minor formatting fix surfaced by Biome on already-committed Task 1 code (`multiRuleSchema.isPrimary.describe()` line-wrap) — fixed via `biome format --write` and committed as T-04 style.

## Commits

- `eb755a3b0` feat(08.5): T-01 add Anthropic prompt caching to classifier paths *(pre-existing from prior session)*
- `2f8cd2a23` test(08.5): T-02 assert request shape for Anthropic vs non-Anthropic classifier paths
- `a78e55d63` docs(08.5): T-03 document Anthropic prompt caching cut-point for Phase 9 mirror
- `b23ee9eef` style(08.5): T-04 Biome formatting on ai-choose-rule.ts multiRule schema describe()

## Self-Check: PASSED

- ai-choose-rule.ts: FOUND (contains `isAnthropicProvider`, `buildClassifierRequest`, `cacheControl: { type: "ephemeral" }`, `Provider.ANTHROPIC`)
- ai-choose-rule.test.ts: FOUND (4 new OPS-03 tests pass)
- NOTES.md: FOUND (contains `## Prompt Caching` section)
- All 4 commits present in `git log`
