---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - packages/resend/emails/digest-v2.tsx
  - packages/resend/__tests__/digest-v2.test.tsx
autonomous: false
requirements: [DIG-01, DIG-02, DIG-03, DIG-04, DIG-05]
tags: [digest, calendar, react-email]

must_haves:
  truths:
    - "DigestV2Props is extended with optional agenda + calendarActivity fields; existing Phase 4 props shape still type-checks and renders unchanged when both new fields are absent (D-03 backward-compat)"
    - "AgendaSection renders between narrative Section and Urgent section (D-01); CalendarActivitySection renders between Uncertain and the auto-filed roll-ups (D-02)"
    - "Per-event row renders time, optional endTime, title, optional location, and `[⚠ overlaps]` amber pill when overlapWith is non-empty (D-03, D-06, D-09)"
    - "Empty-day fallback strings render as a single italic line inside the corresponding sub-section (D-05)"
    - "Calendar Activity renders sub-headings Review / Added / Confirmed only when their group is non-empty; entire section hidden when all three empty (D-12)"
    - "Each Calendar Activity row renders as a Link with the row's href as the target (D-13)"
    - "All partial-side borders are paired with border-0 companion class (Pattern S4)"
    - "PreviewProps includes sample agenda + calendarActivity data so render-digest-v2.ts produces a Phase-10-shaped preview"
  artifacts:
    - path: "packages/resend/emails/digest-v2.tsx"
      provides: "Extended DigestV2Props + AgendaSection + CalendarActivitySection sub-components"
      contains: "agenda?: AgendaBlock"
  key_links:
    - from: "packages/resend/emails/digest-v2.tsx"
      to: "apps/web/utils/digest/agenda/types.ts"
      via: "type-only import of AgendaBlock"
      pattern: "import type.*AgendaBlock"
    - from: "packages/resend/emails/digest-v2.tsx"
      to: "apps/web/utils/digest/calendar-activity/types.ts"
      via: "type-only import of CalendarActivityBlock"
      pattern: "import type.*CalendarActivityBlock"
---

<objective>
Extend `packages/resend/emails/digest-v2.tsx` in place: add optional `agenda` and `calendarActivity` props (D-03), insert two new sub-components (AgendaSection between narrative and Urgent per D-01; CalendarActivitySection between Uncertain and auto-filed roll-ups per D-02), and extend PreviewProps with fixture data so `render-digest-v2.ts` produces a Phase-10-shaped preview.

Purpose: Lock the visual contract for Wave 3 wiring. Once digest-v2.tsx accepts the new typed props and renders them per D-05 to D-18, Plan 05 just has to populate the props from the pure builders.
Output: 1 modified template file + 1 modified snapshot test + a static HTML preview generated via the dev-loop script + a human-verified visual check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md
@.planning/phases/04-daily-digest/04-CONTEXT.md
@packages/resend/emails/digest-v2.tsx
@packages/resend/__tests__/digest-v2.test.tsx
@packages/resend/scripts/render-digest-v2.ts
@apps/web/utils/digest/agenda/types.ts
@apps/web/utils/digest/calendar-activity/types.ts

<interfaces>
From apps/web/utils/digest/agenda/types.ts (Plan 01):
```ts
export type AgendaItem = { id: string; time: string; endTime: string | null; title: string; location: string | null; isAllDay: boolean; overlapWith: string[] };
export type AgendaBlock = { today: AgendaItem[]; tomorrowMorning: AgendaItem[]; todayFallback: string | null; tomorrowMorningFallback: string | null };
```

From apps/web/utils/digest/calendar-activity/types.ts (Plan 02):
```ts
export type CalendarActivityRow = { sentence: string; href: string };
export type CalendarActivityBlock = { review: CalendarActivityRow[]; added: CalendarActivityRow[]; confirmed: CalendarActivityRow[] };
```

From packages/resend/emails/digest-v2.tsx (existing — lines 30-47 DigestV2Props, lines 116-144 AutoFiledGroupCard analog, lines 178-241 Section composition):
- `DigestV2Props` has: baseUrl, date, sentTime, narrativeGreeting, narrativeBody, urgent, uncertain, autoFiled.
- Sub-component pattern: `<Section className="border-l-[4px] ${colors.border} ${colors.bg} rounded-[3px] py-[14px] px-[16px] pb-[6px]">` with small-caps heading.
- Partial-border idiom (line 135, 203): `border-0 border-t border-solid border-black/5`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DigestV2Props + add AgendaSection + CalendarActivitySection + PreviewProps fixture</name>
  <files>packages/resend/emails/digest-v2.tsx, packages/resend/__tests__/digest-v2.test.tsx</files>
  <read_first>
    - packages/resend/emails/digest-v2.tsx (FULL file, ~396 lines — props, sub-components, render tree, PreviewProps)
    - packages/resend/__tests__/digest-v2.test.tsx (existing snapshot test to extend)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md § "digest-v2.tsx — extend DigestV2Props" + § "Pattern S7" (section insertion)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md D-01, D-02, D-03, D-05, D-06, D-07, D-09, D-11, D-12, D-13, D-18
    - apps/web/utils/digest/agenda/types.ts (Plan 01)
    - apps/web/utils/digest/calendar-activity/types.ts (Plan 02)
    - memory note `react_email_partial_borders` (Pattern S4)
  </read_first>
  <behavior>
    - DigestV2Props gains two optional fields: `agenda?: AgendaBlock | null` and `calendarActivity?: CalendarActivityBlock | null`.
    - When both new fields are absent or null, the render tree matches Phase 4 baseline byte-for-byte (existing snapshot test still passes after fixture-prop extension).
    - AgendaSection renders only when agenda is truthy. It contains:
      - Outer wrapper `<Section className="pt-[28px] px-[32px] pb-[4px]">` matching existing section pattern.
      - Heading `"TODAY"` (small caps style — same Tailwind classes as existing section headings at line 193/213/236).
      - For each `today` AgendaItem: time / optional endTime / title / location / overlap pill.
      - When `todayFallback` is non-null and `today` is empty: render the fallback as a single italic line.
      - Sub-heading `"TOMORROW MORNING"` rendered the same way, with its own items + fallback.
    - CalendarActivitySection renders only when calendarActivity is truthy AND at least one of review/added/confirmed is non-empty (D-12).
      - Single bordered card (NOT three cards per D-18); teal/slate palette via `bg-teal-50` + `border-l-[4px] border-l-teal-400` (or equivalent — designer choice within teal/slate per D-18).
      - Sub-headings "Review", "Added", "Confirmed" render only when their respective array is non-empty (D-12).
      - Each row renders as `<Link href={row.href} ...>{row.sentence}</Link>` — React Email Link auto-escapes children (T-10-02 mitigation).
    - Overlap pill renders as `<span className="bg-amber-100 text-amber-800 rounded px-[6px] py-[1px] text-[11px] font-semibold ml-[6px]">[⚠ overlaps]</span>` when AgendaItem.overlapWith is non-empty.
    - All partial-side borders paired with `border-0` companion class (Pattern S4 hard rule).
    - PreviewProps extended with sample agenda + calendarActivity fixture data (3 today events including one overlapping pair, 1 tomorrow-morning event, 1 review + 1 added + 1 confirmed reconciliation row).
    - No `dangerouslySetInnerHTML` anywhere in the file (T-10-02 hard gate).
  </behavior>
  <action>
    Edit `packages/resend/emails/digest-v2.tsx`:

    1. Add type-only imports at top (after existing imports): `import type { AgendaBlock, AgendaItem } from "@/utils/digest/agenda/types";` and `import type { CalendarActivityBlock, CalendarActivityRow } from "@/utils/digest/calendar-activity/types";`. If the path alias `@/` does not resolve from `packages/resend`, use a relative path via `../../../apps/web/utils/...` OR copy/re-export the types in a `packages/resend/src/types.ts` file (Claude's discretion — pick the path that avoids TS rootDir errors; consult existing imports in digest-v2.tsx to see how cross-package paths are handled).

    2. Extend `DigestV2Props` (lines ~30-47) by appending: `agenda?: AgendaBlock | null;` and `calendarActivity?: CalendarActivityBlock | null;`.

    3. Add new sub-component `AgendaSection({ agenda }: { agenda: AgendaBlock })` inside the file (mirror AutoFiledGroupCard at lines 116-144). Render two day blocks (Today, Tomorrow morning). For each day:
       - Heading "TODAY" / "TOMORROW MORNING" using existing small-caps style: `<Text className="m-0 mb-[12px] text-[11px] font-bold tracking-[0.12em] uppercase text-gray-500">TODAY</Text>`.
       - If items.length > 0: map each AgendaItem → `<Text>` row containing `<span className="font-semibold text-gray-900 mr-[8px]">{item.time}{item.endTime ? `–${item.endTime}` : ''}</span>{item.title}` + optional `<span className="text-gray-500 ml-[8px]">{item.location}</span>` + overlap pill when overlapWith.length > 0.
       - If items.length === 0 AND fallback string is non-null: `<Text className="italic text-gray-500 text-[14px]">{fallback}</Text>`.
       - Between rows use `border-0 border-t border-solid border-black/5` on `i > 0` (Pattern S4).

    4. Add new sub-component `CalendarActivitySection({ block }: { block: CalendarActivityBlock })`. Render a single bordered card with teal/slate palette: `<Section className="border-l-[4px] border-l-teal-400 bg-teal-50 rounded-[3px] py-[14px] px-[16px] pb-[6px]">`. Inside: section title "CALENDAR ACTIVITY" then three sub-blocks (Review/Added/Confirmed), each rendered only when its array is non-empty:
       - Sub-heading `<Text className="m-0 mt-[10px] mb-[6px] text-[12px] font-bold tracking-[0.06em] uppercase text-slate-700">Review</Text>` (etc).
       - Each row: `<Text><Link href={row.href} className="text-slate-700 underline">{row.sentence}</Link></Text>`. Link is from `@react-email/components`.

    5. Modify the main render tree (lines ~178-241):
       - After narrative `<Section>` (line ~179) and before Urgent section: `{props.agenda ? <AgendaSection agenda={props.agenda} /> : null}`.
       - After Uncertain section (line ~211) and before autoFiled map (line ~231): `{props.calendarActivity && (props.calendarActivity.review.length > 0 || props.calendarActivity.added.length > 0 || props.calendarActivity.confirmed.length > 0) ? <CalendarActivitySection block={props.calendarActivity} /> : null}`.

    6. Extend `DigestV2Email.PreviewProps` (lines ~282-395) by adding:
       - `agenda: { today: [<3 items, including two with overlapping times>], tomorrowMorning: [<1 item>], todayFallback: null, tomorrowMorningFallback: null }` — use realistic personal-logistics data (e.g. "Pediatrician visit", "Camp pickup").
       - `calendarActivity: { review: [{ sentence: "REI: looks like it's about Camping reservation rescheduled — review →", href: "https://mail.google.com/mail/u/0/#inbox/abc" }], added: [{ sentence: "Added Dentist Mon at 9:00a to your calendar (from Smile Dental) →", href: "https://calendar.google.com/event/xyz" }], confirmed: [{ sentence: "Orlando Health confirmed Dr. Jones visit — already on your calendar", href: "https://calendar.google.com/event/qrs" }] }`.

    7. Verify nowhere in the file uses `dangerouslySetInnerHTML` (T-10-02 hard gate).

    Edit `packages/resend/__tests__/digest-v2.test.tsx`:
    - Update the snapshot fixture: when an existing test asserts the Phase 4 prop shape without agenda/calendarActivity, leave it alone (D-03 backward-compat) — the snapshot may need regenerating once for the section ordering wrapper. Add a NEW `describe("DigestV2Email — Phase 10 sections", ...)` block with two `it()` cases:
       - "renders AgendaSection when agenda prop is provided" — renders with the PreviewProps agenda, assert the heading "TODAY" appears in the output HTML.
       - "hides CalendarActivitySection when all three groups are empty" — renders with calendarActivity = `{ review: [], added: [], confirmed: [] }`, assert "CALENDAR ACTIVITY" does NOT appear in output.
    - Use `render` from `@react-email/render` (already imported in existing test).
  </action>
  <verify>
    <automated>pnpm --filter @inboxzero/resend test -- digest-v2</automated>
    <automated>grep -c "dangerouslySetInnerHTML" packages/resend/emails/digest-v2.tsx</automated>
  </verify>
  <done>digest-v2 snapshot + new Phase 10 tests pass. dangerouslySetInnerHTML grep returns 0. AgendaSection + CalendarActivitySection exported (or co-located inline). PreviewProps includes agenda + calendarActivity sample data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate static HTML preview via render-digest-v2.ts</name>
  <files>.planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html</files>
  <read_first>
    - packages/resend/scripts/render-digest-v2.ts (full file — confirm output path; PATTERNS notes it writes to `.planning/phases/04-daily-digest/digest-v2-rendered.html`)
    - .planning/phases/04-daily-digest/digest-v2-rendered.html (existing baseline for visual diff)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md § "Render/Preview Dev Loop"
  </read_first>
  <behavior>
    - Running the render script produces a non-empty HTML file containing the new Agenda + Calendar Activity sections in the correct insertion order (per D-01 + D-02).
    - The rendered HTML contains the literal strings "TODAY", "TOMORROW MORNING", "CALENDAR ACTIVITY", at least one sentence template from D-11 ("confirmed" / "Added" / "review"), and the overlap pill marker "overlaps".
  </behavior>
  <action>
    Run `pnpm --filter @inboxzero/resend tsx scripts/render-digest-v2.ts`. If the script writes to `.planning/phases/04-daily-digest/digest-v2-rendered.html` (Phase 4 location), copy the output to `.planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html` to preserve the Phase 10 baseline without overwriting Phase 4's. Do NOT modify the render script (D-03 — backward compat).

    Verify the HTML contains the expected markers (grep):
    - "TODAY" present
    - "TOMORROW MORNING" present
    - "CALENDAR ACTIVITY" present
    - "overlaps" present (overlap pill text)
    - At least one of "confirmed" / "Added" / "review" present
  </action>
  <verify>
    <automated>test -s .planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html && grep -c "TODAY\|TOMORROW MORNING\|CALENDAR ACTIVITY\|overlaps" .planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html</automated>
  </verify>
  <done>HTML file exists at the Phase 10 path with non-zero size; grep finds at least 4 of the expected markers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of rendered digest preview</name>
  <what-built>
    AgendaSection (Today + Tomorrow morning, with overlap pill and empty-day fallback support) and CalendarActivitySection (Review/Added/Confirmed sub-sections, single bordered teal/slate card) added to digest-v2.tsx. PreviewProps fixture extended. Static HTML preview generated at `.planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html`.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/10-digest-agenda-reconciliation-outcomes/digest-v2-phase10-rendered.html` in a browser (Chrome or Firefox).
    2. Confirm the section ordering top-to-bottom: narrative greeting → TODAY agenda → TOMORROW MORNING agenda → URGENT → UNCERTAIN → CALENDAR ACTIVITY → auto-filed groups (receipts/newsletters/marketing/notifications).
    3. Confirm the overlap pill (`[⚠ overlaps]`) is visible on the two overlapping rows in TODAY's agenda, with an amber background.
    4. Confirm CALENDAR ACTIVITY renders as ONE bordered card (teal/slate palette) with three sub-headings "Review" / "Added" / "Confirmed" inside it — NOT as three separate cards.
    5. Confirm no rogue thick borders (~3px) on any side of any new section (Pattern S4 — partial-border companion class working correctly).
    6. Confirm the sentence shapes match D-11 verbatim (Review: `"... looks like it's about ... — review →"`; Added: `"Added ... to your calendar (from ...) →"`; Confirmed: `"... confirmed ... — already on your calendar"`).
    7. Confirm narrative section + auto-filed groups still look like the Phase 4 baseline (compare side-by-side with `.planning/phases/04-daily-digest/digest-v2-rendered.html`).

    If anything renders broken — wrong colors, thick borders, missing sections, pill not amber, sentences not matching D-11 — report it and pause.
  </how-to-verify>
  <resume-signal>Type "approved" if the visual matches the spec, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgendaItem.title / location → rendered email body | Originate from Google Calendar event data (semi-trusted external source); flow into React Email render which auto-escapes. |
| CalendarActivityRow.sentence / href → rendered email body | sentence is pre-built plain text (Plan 02); href is constructed via pickLinkTarget (Plan 02 — URL-encoded). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | HTML/email-injection via extractedTitle / event title / sender display name | mitigate | React Email `<Text>` and `<Link>` auto-escape children; hard gate: grep for `dangerouslySetInnerHTML` MUST return 0 in the verify step |
| T-10-03 | Tampering | Open-redirect via crafted href | mitigate | href comes only from Plan 02's pickLinkTarget which (a) returns Google's own htmlLink (Google-controlled domain) or (b) constructs a Gmail URL with encodeURIComponent(threadId) |
| T-10-pill | Tampering | Gmail strips overlap pill CSS | mitigate | Pill uses single-color bg + padding + font-weight only (proven email-safe per RESEARCH "Email-Safe Pill Rendering"); verified visually in Task 3 and end-to-end in Plan 05 via send-digest-v2-test.ts |
| T-10-borders | Tampering | Partial borders render as 3px on other sides in Gmail | mitigate | Pattern S4 — every border-t/border-l paired with border-0; verified in Task 3 visual check |
</threat_model>

<verification>
- Task 1 vitest output: digest-v2 test file passes including new Phase 10 cases.
- `grep -c "dangerouslySetInnerHTML" packages/resend/emails/digest-v2.tsx` returns 0.
- `grep -c "border-l-\[4px\]\|border-t\|border-l-" packages/resend/emails/digest-v2.tsx` — each line containing one of these patterns should also contain `border-0` (manual visual diff inspection of changed regions).
- Task 2 rendered HTML exists and contains expected section markers.
- Task 3 human approval received.
</verification>

<success_criteria>
- DigestV2Props.agenda and DigestV2Props.calendarActivity declared (both optional).
- New sub-components render per D-01..D-18; backward compat preserved (Phase 4 props shape still renders unchanged).
- Static HTML preview generated and human-approved.
- No `dangerouslySetInnerHTML` introduced.
</success_criteria>

<output>
Create `.planning/phases/10-digest-agenda-reconciliation-outcomes/10-04-SUMMARY.md` when done.
</output>
