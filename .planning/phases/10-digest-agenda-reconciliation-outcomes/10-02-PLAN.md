---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/utils/digest/calendar-activity/types.ts
  - apps/web/utils/digest/calendar-activity/render-sentence.ts
  - apps/web/utils/digest/calendar-activity/render-sentence.test.ts
  - apps/web/utils/digest/calendar-activity/pick-link-target.ts
  - apps/web/utils/digest/calendar-activity/pick-link-target.test.ts
autonomous: true
requirements: [DIG-05]
tags: [digest, calendar, reconciliation, react-email]

must_haves:
  truths:
    - "AMBIGUOUS row renders sentence shape '{Sender}: looks like it''s about {extractedTitle} — review →' per D-11"
    - "CREATED row renders sentence shape 'Added {extractedTitle} {day} at {time} to your calendar (from {sender}) →' per D-11"
    - "MATCHED row renders sentence shape '{Sender} confirmed {extractedTitle} — already on your calendar' per D-11"
    - "Link target picker returns googleEventHtmlLink for MATCHED/CREATED when non-null; falls back to Gmail thread URL otherwise; AMBIGUOUS always returns Gmail thread URL per D-13"
  artifacts:
    - path: "apps/web/utils/digest/calendar-activity/types.ts"
      provides: "CalendarActivityRow, CalendarActivityBlock types consumed by build-activity + digest-v2.tsx"
      contains: "export type CalendarActivityRow"
    - path: "apps/web/utils/digest/calendar-activity/render-sentence.ts"
      provides: "renderSentence pure function (D-11 templates)"
      contains: "export function renderSentence"
    - path: "apps/web/utils/digest/calendar-activity/pick-link-target.ts"
      provides: "pickLinkTarget pure function (D-13 selection)"
      contains: "export function pickLinkTarget"
  key_links:
    - from: "apps/web/utils/digest/calendar-activity/render-sentence.ts"
      to: "apps/web/utils/digest/agenda/format-time.ts"
      via: "import formatAgendaTime for CREATED day/time rendering"
      pattern: "from.*agenda/format-time"
---

<objective>
Establish the calendar-activity pure helpers: typed row contracts, the D-11 sentence-template renderer (Review/Added/Confirmed), and the D-13 link-target selector. All pure functions, no I/O, fixture-table tested.

Purpose: Lock the Calendar Activity rendering contracts in Wave 1 so Plan 03's props builder and Plan 04's React Email sub-component can both consume stable shapes.
Output: 3 pure source files + 2 vitest fixture-table suites covering all DIG-05 sentence branches and link-target branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md
@.planning/phases/09-email-calendar-reconciliation/09-CONTEXT.md
@apps/web/utils/calendar/reconciliation/dice.ts
@apps/web/utils/calendar/reconciliation/match.test.ts
@apps/web/prisma/schema.prisma

<interfaces>
From apps/web/prisma/schema.prisma (ReconciliationRecord model, lines 898-927):
- `outcome` enum values used in Plan 10: `MATCHED | CREATED | AMBIGUOUS` (FAILED/PENDING excluded per D-16).
- Relevant fields: `extractedTitle: string` (NOT NULL), `extractedStart: DateTime` (NOT NULL), `extractedEnd: DateTime?`, `extractedLocation: string?`, `extractedIsAllDay: Boolean?` (Prisma field name — note `extracted` prefix), `threadId: string`, `googleEventHtmlLink: string?`, `createdAt: DateTime`, `emailAccountId: string`, `messageId: string`.

From apps/web/utils/digest/agenda/format-time.ts (created in Plan 01, not yet present — declare import path; Plan 03 wave-gates ordering):
```ts
export function formatAgendaTime({ iso: string, isAllDay: boolean }): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + pick-link-target.ts + tests</name>
  <files>apps/web/utils/digest/calendar-activity/types.ts, apps/web/utils/digest/calendar-activity/pick-link-target.ts, apps/web/utils/digest/calendar-activity/pick-link-target.test.ts</files>
  <read_first>
    - apps/web/utils/calendar/reconciliation/match.ts (decision-tree shape, full file ~82 lines)
    - apps/web/utils/calendar/reconciliation/dice.ts (pure-helper file shape with header docstring)
    - apps/web/utils/calendar/reconciliation/match.test.ts lines 1-40 (test boilerplate)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md D-13
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md § "pick-link-target.ts"
    - apps/web/prisma/schema.prisma lines containing `model ReconciliationRecord` and `enum ReconciliationOutcome`
  </read_first>
  <behavior>
    - pickLinkTarget({ outcome: "MATCHED", googleEventHtmlLink: "https://calendar.google.com/event/abc", threadId: "T1" }) returns "https://calendar.google.com/event/abc"
    - pickLinkTarget({ outcome: "CREATED", googleEventHtmlLink: "https://calendar.google.com/event/xyz", threadId: "T2" }) returns "https://calendar.google.com/event/xyz"
    - pickLinkTarget({ outcome: "MATCHED", googleEventHtmlLink: null, threadId: "T3" }) returns "https://mail.google.com/mail/u/0/#inbox/T3" (fallback per D-13)
    - pickLinkTarget({ outcome: "CREATED", googleEventHtmlLink: null, threadId: "T4" }) returns "https://mail.google.com/mail/u/0/#inbox/T4"
    - pickLinkTarget({ outcome: "AMBIGUOUS", googleEventHtmlLink: "anything", threadId: "T5" }) returns "https://mail.google.com/mail/u/0/#inbox/T5" (AMBIGUOUS always Gmail per D-13)
    - threadId is URL-encoded via encodeURIComponent in the Gmail link template (T-10-03 mitigation)
  </behavior>
  <action>
    Create `apps/web/utils/digest/calendar-activity/types.ts` exporting:
    - `CalendarActivityRow` type: `{ sentence: string; href: string }`.
    - `CalendarActivityBlock` type: `{ review: CalendarActivityRow[]; added: CalendarActivityRow[]; confirmed: CalendarActivityRow[] }`.
    - `CalendarActivityOutcome` type alias: `"MATCHED" | "CREATED" | "AMBIGUOUS"` (FAILED/PENDING excluded per D-16; do NOT export those values).

    Create `pick-link-target.ts` exporting `pickLinkTarget({ outcome, googleEventHtmlLink, threadId }: { outcome: CalendarActivityOutcome; googleEventHtmlLink: string | null; threadId: string }): string`. Logic per D-13:
    - If outcome !== "AMBIGUOUS" AND googleEventHtmlLink is truthy → return googleEventHtmlLink.
    - Otherwise return `` `https://mail.google.com/mail/u/0/#inbox/${encodeURIComponent(threadId)}` ``.
    - Header docstring matches dice.ts pure-helper style; cite D-13.

    Create `pick-link-target.test.ts` with `describe("pickLinkTarget — D-13 link selection", ...)` and one `it()` per behavior bullet. Include explicit test "URL-encodes threadId in Gmail fallback link (T-10-03)".
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- utils/digest/calendar-activity/pick-link-target.test.ts</automated>
  </verify>
  <done>pick-link-target.test.ts passes with all 6 assertions green. types.ts exports CalendarActivityRow, CalendarActivityBlock, CalendarActivityOutcome. No imports outside file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render-sentence.ts + tests</name>
  <files>apps/web/utils/digest/calendar-activity/render-sentence.ts, apps/web/utils/digest/calendar-activity/render-sentence.test.ts</files>
  <read_first>
    - apps/web/utils/calendar/reconciliation/dice.ts (pure single-function file shape)
    - apps/web/utils/calendar/reconciliation/match.test.ts (fixture-table style)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md D-11, D-17
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md "Open Questions #3" (day/time format recommendation)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md § "render-sentence.ts"
    - apps/web/utils/digest/agenda/format-time.ts (created in Plan 01; this task imports `formatAgendaTime`)
  </read_first>
  <behavior>
    - renderSentence({ outcome: "MATCHED", sender: "REI", extractedTitle: "Camping reservation", extractedStart: new Date("2026-05-25T17:00:00Z"), isAllDay: false }) returns "REI confirmed Camping reservation — already on your calendar"
    - renderSentence({ outcome: "CREATED", sender: "Orlando Health", extractedTitle: "Dr. Jones visit", extractedStart: <2026-05-25 9am ET>, isAllDay: false }) returns "Added Dr. Jones visit Mon at 9:00a to your calendar (from Orlando Health) →"
    - renderSentence({ outcome: "AMBIGUOUS", sender: "Camp Director", extractedTitle: "rescheduled session", extractedStart: <any>, isAllDay: false }) returns "Camp Director: looks like it's about rescheduled session — review →"
    - renderSentence handles isAllDay: true for CREATED by omitting time, e.g. "Added School Holiday Mon to your calendar (from School) →"
    - All output is plain text; no HTML; no `<` or `>` characters injected from input fields (T-10-02 — React Email auto-escapes downstream but the helper does not pre-escape).
  </behavior>
  <action>
    Create `apps/web/utils/digest/calendar-activity/render-sentence.ts`:
    - Import `formatAgendaTime` from `@/utils/digest/agenda/format-time` (Plan 01 dependency — wave gates this; verify the file exists before merge).
    - Import `CalendarActivityOutcome` from `./types`.
    - Private helper `formatDayAbbrev(date: Date): string` using `new Intl.DateTimeFormat("en-US", { timeZone: "America/New_York", weekday: "short" }).format(date)` → "Mon", "Tue", etc.
    - Export `renderSentence({ outcome, sender, extractedTitle, extractedStart, isAllDay }: { outcome: CalendarActivityOutcome; sender: string; extractedTitle: string; extractedStart: Date; isAllDay: boolean }): string`. extractedTitle and extractedStart are non-null per Prisma schema (ReconciliationRecord.extractedTitle is NOT NULL; extractedStart is NOT NULL).
    - Sentence templates per D-11 verbatim.
    - CREATED day/time rendering: when isAllDay → just `${dayAbbrev}`; otherwise → `${dayAbbrev} at ${formatAgendaTime({ iso: extractedStart.toISOString(), isAllDay: false })}`.
    - Header docstring lists all three D-11 templates verbatim (copy from CONTEXT D-11) + cite RESEARCH "Open Questions #3" recommendation.

    Create `render-sentence.test.ts` with `describe("renderSentence — D-11 sentence templates", ...)` and one `it()` per behavior bullet plus:
    - "does not include HTML metacharacters from extracted fields raw" (assert output equals expected string exactly when input contains "<" — the helper passes it through; React Email escapes downstream)
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- utils/digest/calendar-activity/render-sentence.test.ts</automated>
  </verify>
  <done>render-sentence.test.ts passes; all 6 assertions green; sentence shapes match D-11 verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciliation field → Calendar Activity sentence | extractedTitle / extractedLocation / sender display name originate from external email content (semi-trusted); flow into rendered email body. |
| threadId → Gmail URL | threadId is from Gmail API (trusted Google source) but URL-encoded as defense-in-depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering / Info Disclosure | HTML/email-injection via extractedTitle/sender into render-sentence output | mitigate | renderSentence returns plain text; React Email (Plan 04) auto-escapes via `<Text>`/`<Link>` children; verified by test "does not include HTML metacharacters raw" + Plan 04 grep gate for `dangerouslySetInnerHTML` |
| T-10-03 | Tampering | Gmail URL construction with user-influenced threadId | mitigate | pickLinkTarget uses `encodeURIComponent(threadId)`; tested explicitly |
| T-10-01 | Tampering | Sonnet hallucination — N/A in Plan 02 | transfer | Plan 05 handles |
</threat_model>

<verification>
- Both test files green.
- `pick-link-target.ts` source contains literal substring `encodeURIComponent` (T-10-03 evidence): `grep -c "encodeURIComponent" apps/web/utils/digest/calendar-activity/pick-link-target.ts` must be >= 1.
- `render-sentence.ts` does NOT contain `dangerouslySetInnerHTML` or template-string HTML tags: `grep -c "dangerouslySetInnerHTML\|<[a-zA-Z]" apps/web/utils/digest/calendar-activity/render-sentence.ts` must be 0.
- No Prisma/Google/React imports in either source file.
</verification>

<success_criteria>
- types.ts, render-sentence.ts, pick-link-target.ts created and exported.
- Both test files pass: `cd apps/web && pnpm test -- utils/digest/calendar-activity/`
- All D-11 sentence shapes locked verbatim; D-13 link-target rules complete; T-10-02 + T-10-03 mitigations in place.
</success_criteria>

<output>
Create `.planning/phases/10-digest-agenda-reconciliation-outcomes/10-02-SUMMARY.md` when done.
</output>
