---
phase: 10
plan: 05
type: execute
wave: 3
depends_on: [10-03, 10-04]
files_modified:
  - apps/web/utils/digest/run-daily-digest.ts
  - apps/web/utils/digest/run-daily-digest.test.ts
  - apps/web/utils/ai/digest/digest-prompt.ts
  - apps/web/utils/ai/digest/digest-prompt.test.ts
  - apps/web/utils/ai/digest/generate-digest-content.ts
autonomous: false
requirements: [DIG-01, DIG-02, DIG-03, DIG-04, DIG-05]
tags: [digest, calendar, ai, sonnet, prompt, reconciliation]

must_haves:
  truths:
    - "runDailyDigest fetches getUpcomingEvents and ReconciliationRecord rows in parallel via Promise.allSettled; either fetch failing logs a warn and degrades that block only — digest still sends (D-25, D-26)"
    - "Sender map is built via a single Gmail batch fetch (emailProvider.getMessagesBatch) for messageIds in reconciliation rows not already in the existing messageMap (RESEARCH Option A)"
    - "DigestV2Props receives populated agenda + calendarActivity built by buildAgenda + buildCalendarActivity"
    - "DIGEST_SYSTEM_PROMPT contains the literal hard-rule string per D-22: 'Only reference events / reconciliations present in the AGENDA and RECONCILIATIONS blocks. Do not infer, summarize counts you can't see, or extrapolate.'"
    - "buildDigestPrompt accepts agendaCompact + reconciliationsCompact params and emits ### AGENDA + ### RECONCILIATIONS sections; both render '(nothing on the calendar)' / '(none in the last 24h)' when empty"
    - "Token-delta measurement: a fixture-based test asserts buildDigestPrompt output grows by ≤ 1000 estimated tokens (chars/4) vs an identical call with empty agenda+reconciliations (D-20)"
    - "Reconciliation Prisma query uses the index (emailAccountId, createdAt DESC) — verified by the query shape matching D-24 exactly"
  artifacts:
    - path: "apps/web/utils/digest/run-daily-digest.ts"
      provides: "Parallel-fetch + sender-map + props composition wiring"
      contains: "Promise.allSettled"
    - path: "apps/web/utils/ai/digest/digest-prompt.ts"
      provides: "Extended DIGEST_SYSTEM_PROMPT + buildDigestPrompt with AGENDA/RECONCILIATIONS sections"
      contains: "### AGENDA"
  key_links:
    - from: "apps/web/utils/digest/run-daily-digest.ts"
      to: "apps/web/utils/digest/agenda/build-agenda.ts"
      via: "imports buildAgenda"
      pattern: "from.*agenda/build-agenda"
    - from: "apps/web/utils/digest/run-daily-digest.ts"
      to: "apps/web/utils/digest/calendar-activity/build-activity.ts"
      via: "imports buildCalendarActivity"
      pattern: "from.*calendar-activity/build-activity"
    - from: "apps/web/utils/digest/run-daily-digest.ts"
      to: "apps/web/utils/calendar/upcoming-events.ts"
      via: "imports getUpcomingEvents (Phase 8 cached read path)"
      pattern: "from.*calendar/upcoming-events"
    - from: "apps/web/utils/digest/run-daily-digest.ts"
      to: "prisma.reconciliationRecord"
      via: "Prisma findMany with D-24 query shape"
      pattern: "reconciliationRecord\\.findMany"
---

<objective>
Wire the Wave 1/2 helpers into the live digest pipeline. Add the parallel-fetch + sender-map lookup + props composition to `run-daily-digest.ts`. Extend `digest-prompt.ts` with AGENDA + RECONCILIATIONS context blocks plus the D-22 hard rule. Thread the new compact data through `generate-digest-content.ts`. Add an automated token-delta gate ≤ +1000 tokens/digest (D-20).

Purpose: This is the wiring wave. After this plan ships, the next 9am ET digest send produces a Phase-10-shaped email end-to-end.
Output: Modified orchestrator + extended prompt + extended Sonnet call site + new integration test + new prompt-delta test. Manual checkpoint at end for real-Gmail visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md
@.planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md
@.planning/phases/04-daily-digest/04-CONTEXT.md
@apps/web/utils/digest/run-daily-digest.ts
@apps/web/utils/ai/digest/digest-prompt.ts
@apps/web/utils/ai/digest/generate-digest-content.ts
@apps/web/utils/ai/digest/digest-schema.ts
@apps/web/utils/calendar/upcoming-events.ts
@apps/web/utils/calendar/reconciliation/index.ts

<interfaces>
From apps/web/utils/calendar/upcoming-events.ts (Phase 8):
```ts
export async function getUpcomingEvents({ emailAccountId: string; now: Date; logger: Logger }): Promise<NormalizedCalendarEvent[]>;
// Never throws — returns [] on cold-cache + API failure, stale on warm-cache + API failure.
```

From apps/web/utils/digest/agenda/build-agenda.ts (Plan 03):
```ts
export function buildAgenda({ events: NormalizedCalendarEvent[]; now: Date }): AgendaBlock;
```

From apps/web/utils/digest/calendar-activity/build-activity.ts (Plan 03):
```ts
export function buildCalendarActivity({ records: ReconciliationInput[]; senderMap: Map<string, string> }): CalendarActivityBlock | null;
```

From apps/web/utils/ai/digest/digest-prompt.ts (existing):
- `DIGEST_SYSTEM_PROMPT` — string constant.
- `buildDigestPrompt({ todayDate, bucketed }): string` — joins array of section strings via "\n".
- `renderBucket(label, items)` — helper used by builder.

From apps/web/utils/ai/digest/generate-digest-content.ts (existing):
- `generateDigestContent({ emailAccount, todayDate, bucketed }): Promise<...>` — calls `generateObject` with DIGEST_SYSTEM_PROMPT + buildDigestPrompt.

From apps/web/utils/digest/run-daily-digest.ts (existing, lines referenced from PATTERNS):
- Line 212: existing `emailProvider.getMessagesBatch(...)` call building `messageMap`.
- Line 254: `generateDigestContent({...})` call site.
- Line 281: sender regex `/^(.*?)(?:\s*<([^>]+)>)?$/.exec(src.from)`.
- Line 305-319: props composition for DigestV2Props.
- Line 367-381: outer try/catch on per-account loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend digest-prompt.ts with AGENDA + RECONCILIATIONS sections + D-22 hard rule + token-delta test</name>
  <files>apps/web/utils/ai/digest/digest-prompt.ts, apps/web/utils/ai/digest/digest-prompt.test.ts</files>
  <read_first>
    - apps/web/utils/ai/digest/digest-prompt.ts (FULL file, ~84 lines — DIGEST_SYSTEM_PROMPT + renderBucket + buildDigestPrompt)
    - apps/web/utils/ai/digest/digest-schema.ts (Sonnet output schema — verify NO change needed)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md D-19, D-20, D-21, D-22
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md § "digest-prompt.ts"
    - apps/web/utils/digest/agenda/types.ts (AgendaBlock shape from Plan 01)
    - apps/web/utils/digest/calendar-activity/types.ts (Plan 02)
  </read_first>
  <behavior>
    - DIGEST_SYSTEM_PROMPT (or a new appended block) contains the literal D-22 hard-rule paragraph verbatim: "Only reference events / reconciliations present in the AGENDA and RECONCILIATIONS blocks. Do not infer, summarize counts you can't see, or extrapolate."
    - DIGEST_SYSTEM_PROMPT references that voice guardrails (humor drop for grief/illness/legal/distress) apply to AGENDA + RECONCILIATIONS content as well (D-21).
    - DIGEST_SYSTEM_PROMPT instructs "weave 1-2 references in naturally if they fit the morning's narrative; never enumerate them" (D-19).
    - buildDigestPrompt signature changes from `({ todayDate, bucketed })` to `({ todayDate, bucketed, agendaCompact, reconciliationsCompact })` where:
      - `agendaCompact: { day: "today" | "tomorrow"; time: string; title: string }[]` — flat array (today + tomorrowMorning combined, all-day labeled "All day"), pre-built by caller in run-daily-digest. The `day` field disambiguates the two windows so Sonnet can correctly attribute "9am" to today vs tomorrow morning.
      - `reconciliationsCompact: { outcome: "MATCHED" | "CREATED" | "AMBIGUOUS"; title: string; sender: string }[]`.
    - buildDigestPrompt emits `### AGENDA` section listing each compact entry as `- [{day}] {time} {title}` (or "(nothing on the calendar)" when empty).
    - buildDigestPrompt emits `### RECONCILIATIONS` section listing each as `- [{outcome}] {title} — {sender}` (or "(none in the last 24h)" when empty).
    - Token-delta test: calling buildDigestPrompt with 5 agenda entries + 5 reconciliation entries grows the output by ≤ 4000 characters vs the empty-arrays call (chars/4 ≈ ≤ 1000 tokens per D-20).
    - Existing callers must remain compilable: introduce defaults `agendaCompact = []` and `reconciliationsCompact = []` so the signature change is backward-compat at the type level for any test that has not yet been updated.
  </behavior>
  <action>
    Edit `apps/web/utils/ai/digest/digest-prompt.ts`:

    1. Append new instruction block to `DIGEST_SYSTEM_PROMPT` (before the existing "Output JSON..." or final-line instruction). Use the exact verbiage from PATTERNS § "digest-prompt.ts" / CONTEXT D-22:
       ```
       AGENDA + RECONCILIATIONS HANDLING
       - Only reference events / reconciliations present in the AGENDA and RECONCILIATIONS blocks. Do not infer, summarize counts you can't see, or extrapolate.
       - Weave 1-2 references in naturally if they fit the morning's narrative; never enumerate them.
       - The voice guardrails above (drop humor for grief / serious illness / financial distress / legal threats / family emergencies) apply to AGENDA and RECONCILIATIONS content too.
       ```

    2. Add two new pure-string helpers (mirror `renderBucket` shape):
       - `function renderAgenda(items: { day: "today" | "tomorrow"; time: string; title: string }[]): string` → returns `### AGENDA\n(nothing on the calendar)\n` when empty, else `### AGENDA\n` + items joined by `\n` as `- [${day}] ${time} ${title}` + trailing `\n`.
       - `function renderReconciliations(items: { outcome: string; title: string; sender: string }[]): string` → returns `### RECONCILIATIONS\n(none in the last 24h)\n` when empty, else `### RECONCILIATIONS\n` + items joined by `\n` as `- [${outcome}] ${title} — ${sender}` + trailing `\n`.

    3. Change `buildDigestPrompt` signature to:
       `export function buildDigestPrompt({ todayDate, bucketed, agendaCompact = [], reconciliationsCompact = [] }: { todayDate: string; bucketed: <existing type>; agendaCompact?: { day: "today" | "tomorrow"; time: string; title: string }[]; reconciliationsCompact?: { outcome: string; title: string; sender: string }[] }): string`.

    4. Insert `renderAgenda(agendaCompact)` and `renderReconciliations(reconciliationsCompact)` into the joined-array return value, BEFORE the existing `renderBucket("URGENT", ...)` calls so the agenda + reconciliations appear early in the prompt (Pattern P3 — re-entrant string assembly).

    Create `apps/web/utils/ai/digest/digest-prompt.test.ts` with `describe("buildDigestPrompt — Phase 10 extensions", ...)`:
    - "includes ### AGENDA section with empty placeholder when agendaCompact is empty"
    - "includes ### AGENDA section with formatted items when agendaCompact is non-empty"
    - "includes ### RECONCILIATIONS section with empty placeholder when empty"
    - "includes ### RECONCILIATIONS section with formatted items when non-empty"
    - "DIGEST_SYSTEM_PROMPT contains the D-22 hard rule verbatim" (`expect(DIGEST_SYSTEM_PROMPT).toContain("Only reference events / reconciliations present in the AGENDA and RECONCILIATIONS blocks. Do not infer, summarize counts you can't see, or extrapolate.")`)
    - "token-delta: 5 agenda + 5 reconciliations adds ≤ 4000 characters (≤ ~1000 tokens) vs empty arrays" — assertion `expect(longLen - shortLen).toBeLessThanOrEqual(4000)`. Use realistic-length strings (titles ~30 chars, senders ~25 chars).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- utils/ai/digest/digest-prompt.test.ts</automated>
  </verify>
  <done>digest-prompt.test.ts passes; all 6 assertions green including the D-22 verbatim string match and the ≤4000 chars token-delta gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire run-daily-digest.ts — parallel fetches, sender map, props composition, generateDigestContent signature update</name>
  <files>apps/web/utils/digest/run-daily-digest.ts, apps/web/utils/digest/run-daily-digest.test.ts, apps/web/utils/ai/digest/generate-digest-content.ts</files>
  <read_first>
    - apps/web/utils/digest/run-daily-digest.ts (FULL file, ~386 lines — orchestrator, messageMap build at line 212, sender regex at line 281, props composition at line 305-319, outer try/catch at line 367-381)
    - apps/web/utils/digest/run-daily-digest.test.ts (existing test — extend with mocked fetches)
    - apps/web/utils/ai/digest/generate-digest-content.ts (current signature)
    - apps/web/utils/calendar/upcoming-events.ts (getUpcomingEvents signature)
    - apps/web/utils/calendar/reconciliation/index.ts lines 240-260 (`getUpcomingEvents(...).catch(() => [])` idiom)
    - apps/web/utils/digest/agenda/build-agenda.ts (Plan 03)
    - apps/web/utils/digest/calendar-activity/build-activity.ts (Plan 03)
    - apps/web/prisma/schema.prisma (ReconciliationRecord model — confirm Prisma client method name)
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-CONTEXT.md D-23, D-24, D-25, D-26
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-PATTERNS.md § "run-daily-digest.ts"
    - .planning/phases/10-digest-agenda-reconciliation-outcomes/10-RESEARCH.md § "Reconciliation → Sender Name Lookup" (Option A)
  </read_first>
  <behavior>
    - Before the `generateDigestContent` call (line ~254), runDailyDigest issues two fetches in parallel via Promise.allSettled:
      - getUpcomingEvents({ emailAccountId: account.id, now: <new Date()>, logger: scoped })
      - prisma.reconciliationRecord.findMany with the exact D-24 query shape (where: emailAccountId + createdAt gte now-24h + outcome in MATCHED/CREATED/AMBIGUOUS; orderBy: extractedStart asc).
    - On either fetch rejection: `scoped.warn("agenda.fetch.failed" | "reconciliations.fetch.failed", { error: String(...) })` (structured fields only — no extractedTitle/location per Pattern S3), proceed with empty array.
    - After fetches resolve, build a `senderMap: Map<string, string>` by:
      1. Collecting reconciliation rows' `messageId` set.
      2. For messageIds already present in the existing `messageMap` (line ~212), parse the from-header via the inline regex from line 281 and populate the map.
      3. For messageIds NOT in messageMap, call `emailProvider.getMessagesBatch(missingIds)` ONCE, parse `from` for each, populate the map. If this batch call throws, log warn + continue with a partial map (fallback to messageId in build-activity per Plan 03).
    - Build compact data for the Sonnet prompt:
      - `agendaCompact = [...agenda.today, ...agenda.tomorrowMorning].map(i => ({ time: i.time, title: i.title }))` — flat list, all entries.
      - `reconciliationsCompact = [...reconciliations].map(r => ({ outcome: r.outcome, title: r.extractedTitle ?? "(no title)", sender: senderMap.get(r.messageId) ?? r.messageId }))`.
    - Call `generateDigestContent({ ...existing, agendaCompact, reconciliationsCompact })` — extend its signature accordingly to pass through to buildDigestPrompt.
    - Add `agenda` and `calendarActivity` fields to the DigestV2Props composition (line ~305-319).
    - All new I/O is wrapped per D-26: a failure in the new fetch block must NOT prevent the digest from sending. Existing line ~367-381 outer try/catch already covers the per-account loop; the new fetches additionally use Promise.allSettled internally so neither rejection short-circuits the other.
    - Integration test extends `run-daily-digest.test.ts` with mocks for getUpcomingEvents + prisma.reconciliationRecord.findMany + emailProvider.getMessagesBatch, asserts the produced DigestV2Props includes a populated agenda + calendarActivity, AND that a rejection of either fetch results in null/empty corresponding props field without throwing.
  </behavior>
  <action>
    Edit `apps/web/utils/digest/run-daily-digest.ts`:

    1. Add imports at top:
       - `import { getUpcomingEvents } from "@/utils/calendar/upcoming-events";`
       - `import { buildAgenda } from "@/utils/digest/agenda/build-agenda";`
       - `import { buildCalendarActivity } from "@/utils/digest/calendar-activity/build-activity";`
       - Prisma client already imported — verify.

    2. After the existing messageMap build (~line 212) and BEFORE the `generateDigestContent` call (~line 254), insert the parallel-fetch block:
       - `const now = new Date();` (reuse the variable name already in scope if present).
       - `const [eventsR, reconciliationsR] = await Promise.allSettled([...])` with the two calls per RESEARCH "Existing Pattern: Parallel Fetch with Failure Isolation".
       - `const events = eventsR.status === "fulfilled" ? eventsR.value : [];`
       - `const reconciliations = reconciliationsR.status === "fulfilled" ? reconciliationsR.value : [];`
       - On rejection: `scoped.warn("agenda.fetch.failed", { error: String(eventsR.reason) })` / `scoped.warn("reconciliations.fetch.failed", { error: String(reconciliationsR.reason) })`.

    3. Build the senderMap:
       - `const senderMap = new Map<string, string>();`
       - For each reconciliation row whose messageId is in `messageMap`: parse via inline regex `/^(.*?)(?:\s*<([^>]+)>)?$/.exec(msg.from)`, set `senderMap.set(messageId, senderName)`.
       - Collect missing messageIds; if non-empty wrap `await emailProvider.getMessagesBatch(missingIds)` in try/catch, populate map from results, log warn on failure.

    4. Compute `agenda = buildAgenda({ events, now })` and `calendarActivity = buildCalendarActivity({ records: reconciliations.map(r => ({ id: r.id, outcome: r.outcome, extractedTitle: r.extractedTitle, extractedStart: r.extractedStart, threadId: r.threadId, googleEventHtmlLink: r.googleEventHtmlLink, messageId: r.messageId, isAllDay: r.extractedIsAllDay ?? false })), senderMap })`. NOTE: Prisma field is `extractedIsAllDay` (with "extracted" prefix); map to the helper's `isAllDay` here.

    5. Build compact prompt input:
       - `const agendaCompact = [...agenda.today.map(i => ({ day: "today" as const, time: i.time, title: i.title })), ...agenda.tomorrowMorning.map(i => ({ day: "tomorrow" as const, time: i.time, title: i.title }))];` — day tag disambiguates the two windows for Sonnet (avoids "which day is the 9am?" ambiguity).
       - `const reconciliationsCompact = reconciliations.map(r => ({ outcome: r.outcome, title: r.extractedTitle, sender: senderMap.get(r.messageId) ?? r.messageId }));` — extractedTitle is NOT NULL per Prisma schema.

    6. Update the `generateDigestContent({...})` call (line ~254) to pass `agendaCompact` and `reconciliationsCompact` through.

    7. Extend the DigestV2Props composition (line ~305-319) by adding `agenda,` and `calendarActivity,`.

    Edit `apps/web/utils/ai/digest/generate-digest-content.ts`:
    - Extend the function signature to accept `agendaCompact?: { time: string; title: string }[]` and `reconciliationsCompact?: { outcome: string; title: string; sender: string }[]` (both optional, default `[]`).
    - Pass both through to `buildDigestPrompt({ ..., agendaCompact, reconciliationsCompact })`.
    - The digestContentSchema does NOT change (narrative text still the only Sonnet output).

    Edit `apps/web/utils/digest/run-daily-digest.test.ts`:
    - Add mocks for `getUpcomingEvents` and `prisma.reconciliationRecord.findMany` and `emailProvider.getMessagesBatch` if not already.
    - Add `describe("Phase 10 — agenda + calendar activity", ...)` with:
      - "populates props.agenda from getUpcomingEvents result"
      - "populates props.calendarActivity from reconciliationRecord rows + sender map"
      - "degrades gracefully when getUpcomingEvents rejects (props.agenda still set with fallback strings; digest still sends)"
      - "degrades gracefully when reconciliationRecord.findMany rejects (props.calendarActivity is null; digest still sends)"
      - "logs warn with structured fields only on fetch failure" — assert logger.warn was called and the argument does NOT contain extractedTitle/extractedLocation/event details (Pattern S3).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- utils/digest/run-daily-digest.test.ts</automated>
  </verify>
  <done>run-daily-digest.test.ts passes including all 5 Phase 10 cases. generateDigestContent compiles with new optional params. Props composition includes agenda + calendarActivity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end Gmail render + Sonnet token-delta measurement</name>
  <what-built>
    Full digest pipeline now produces Phase-10-shaped emails: parallel fetches for calendar events + reconciliations, sender lookup, agenda + calendar-activity props populated, Sonnet prompt extended with AGENDA + RECONCILIATIONS context blocks + D-22 hard rule.
  </what-built>
  <how-to-verify>
    1. **End-to-end Gmail render (T-10-pill verification).** Run from local dev:
       ```
       RESEND_API_KEY=<key> TEST_TO=rebekah@trueocean.com pnpm --filter @inboxzero/resend tsx scripts/send-digest-v2-test.ts
       ```
       (uses the extended PreviewProps from Plan 04 — sends to your real Gmail). Open the email in real Gmail (web client, not just preview). Confirm:
       - Section ordering: narrative → TODAY → TOMORROW MORNING → URGENT → UNCERTAIN → CALENDAR ACTIVITY → auto-filed.
       - `[⚠ overlaps]` amber pill visible on the two overlapping rows (T-10-pill — Gmail-CSS-stripping check).
       - CALENDAR ACTIVITY renders as one bordered teal/slate card with three sub-headings inside.
       - No thick 3px borders on any side of any new section.
       - Sentence templates match D-11 verbatim.
       - Source-email links are clickable and go to the right Gmail/Calendar URL (D-13).

    2. **Sonnet narrative agenda-awareness check.** Trigger a manual digest send against a real-inbox snapshot OR review the Sonnet output captured during a real 9am ET cron send (check logs / Tinybird). Read the narrative paragraph:
       - Does it reference any of the today/tomorrow agenda items naturally? (D-19 — weaves 1-2 references when natural)
       - Does it invent any event NOT in the AGENDA / RECONCILIATIONS context blocks? (D-22 hard rule — should be NO inventions)
       - Does it drop humor when reconciliation/agenda items touch grief/illness/distress? (D-21 — qualitative check based on the day's data)

    3. **Token-delta measurement (D-20 + OPS-02 gate).** Two-step gate:

       **3a. PRE-MERGE: Capture the Phase 4 baseline before this PR merges.** Tinybird's `ai_usage` `promptTokens` is the source of truth, but baselines decay (prompt edits land between phases). Before opening the PR, run a Tinybird pipe query for the last 3 successful digest sends (filter by `userId=rebekah@trueocean.com` AND `model` matching the Sonnet ID AND `provider=anthropic` AND timestamp >= last 7 days). Record the three `promptTokens` values + their mean in `.planning/phases/10-digest-agenda-reconciliation-outcomes/TOKEN-BASELINE.md`. If Tinybird is unreachable, fall back to scraping local logger output for the last 3 sends — DO NOT proceed without a captured baseline (post-hoc baseline reconstruction is impossible once the prompt change has shipped).

       **3b. POST-DEPLOY: Capture 3 consecutive post-merge digests' `promptTokens` and compute the mean.** Delta = post-mean − pre-mean. Gate: delta ≤ +1000 tokens per digest. If delta exceeds 1000, REJECT — pre-filter or trim AGENDA / RECONCILIATIONS compact payload, redeploy, re-measure. Record both means + delta in the SUMMARY.md for the phase.

    Report each of (1) (2) (3). If (1) or (2) shows issues, describe them and pause. If (3) exceeds +1000 tokens/digest, flag for rollback of Sonnet prompt extension while keeping the React Email render layer (the latter does NOT consume Sonnet tokens).
  </how-to-verify>
  <resume-signal>Type "approved" if all three checks pass, "delta-exceeded" if the token-delta gate fails, or describe other issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email `from` header → sender name in digest body | Already-handled-by-Phase-4 inline regex; same field flow as ActionItemCard. |
| Reconciliation row → Sonnet prompt | extractedTitle / outcome / sender flow into the LLM prompt as structured-text context — same shape as Phase 9's existing reconciliation logging. |
| getUpcomingEvents (failure cascade) | Per Phase 8 contract, never throws. Defense-in-depth Promise.allSettled ensures even an unexpected throw doesn't kill the digest (D-26). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / Info Disclosure | Sonnet narrative hallucinating events / reconciliations not in input | mitigate | D-22 verbatim hard rule appended to DIGEST_SYSTEM_PROMPT (test asserts verbatim presence in Task 1); human-verify check (#2) in Task 3 confirms narrative does not invent events |
| T-10-02 | Tampering | HTML/email-injection via extractedTitle / sender propagating into Sonnet prompt | mitigate | Sonnet receives plain text only; the LLM output is rendered through digest-v2.tsx React Email auto-escape; furthermore the AGENDA/RECONCILIATIONS strings inside the prompt are markdown-prefixed (`- ${time} ${title}`) — no HTML semantics |
| T-10-03 | Tampering | (covered in Plan 02) | mitigate | encodeURIComponent in pickLinkTarget |
| T-10-04 | Resource exhaustion | Runaway prompt growth (token cost explosion) | mitigate | Token-delta test in Task 1 (chars/4 ≤ 1000 gate); end-to-end measurement gate in Task 3 (D-20); rollback path documented if exceeded |
| T-10-05 | Availability | Agenda or reconciliation fetch failure cascading into digest failure | mitigate | Promise.allSettled per-branch isolation in Task 2; existing outer try/catch on per-account loop; integration test asserts digest still sends when either fetch rejects |
| T-10-PII | Info Disclosure | extractedTitle / extractedLocation leaking into logger output on degraded paths | mitigate | Pattern S3 — structured-fields-only logging; test asserts logger.warn argument does NOT include extracted fields |
</threat_model>

<verification>
- Task 1: digest-prompt.test.ts green; D-22 verbatim string assertion passes; ≤4000-char gate passes.
- Task 2: run-daily-digest.test.ts green; 5 Phase 10 cases pass.
- `grep -c "Promise.allSettled" apps/web/utils/digest/run-daily-digest.ts` >= 1.
- `grep -c "buildAgenda\|buildCalendarActivity" apps/web/utils/digest/run-daily-digest.ts` >= 2.
- `grep -c "reconciliationRecord.findMany" apps/web/utils/digest/run-daily-digest.ts` >= 1.
- `grep -c "extractedTitle\|extractedLocation" apps/web/utils/digest/run-daily-digest.ts` returns ONLY counts inside the build-step (not inside any logger.warn arg) — manual visual inspection during code review.
- Full suite green: `cd apps/web && pnpm test -- utils/digest utils/ai/digest` (assumes ~30s runtime per RESEARCH "Sampling Rate").
- Task 3 human approval received covering (1) Gmail render, (2) Sonnet agenda-awareness without invention, (3) ≤1000 tokens/digest delta.
</verification>

<success_criteria>
- Digest send pipeline produces populated agenda + calendarActivity props at runtime (verified via integration test and end-to-end Gmail render).
- Sonnet prompt includes AGENDA + RECONCILIATIONS + D-22 hard rule.
- Token delta gate ≤ +1000 tokens/digest passes via the automated chars/4 test AND the live Tinybird measurement in Task 3.
- All 5 phase requirements (DIG-01..05) are observable in the rendered digest.
- All Plans 01-04 tests still pass alongside Plan 05 changes.
</success_criteria>

<output>
Create `.planning/phases/10-digest-agenda-reconciliation-outcomes/10-05-SUMMARY.md` when done.
</output>
