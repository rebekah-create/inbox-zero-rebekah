---
plan_id: 11-01
phase: 11-calendar-reconciliation-v2
wave: 1
depends_on: []
files_modified:
  - apps/web/prisma/schema.prisma
  - apps/web/prisma/migrations/  # new migration directory
requirements_addressed: [RECv2-04]
autonomous: false  # contains [BLOCKING] migration task
must_haves:
  truths:
    - "ReconciliationOutcome enum in Postgres includes value RESCHEDULE"
    - "ReconciliationRecord row can be created with outcome=RESCHEDULE without DB error"
    - "ReconciliationRecord row can store a non-null rescheduleOfEventId pointing to a Google event id string"
    - "A Prisma migration file is committed under apps/web/prisma/migrations/ that adds these schema changes"
  artifacts:
    - path: apps/web/prisma/schema.prisma
      provides: "ReconciliationOutcome enum extension + rescheduleOfEventId column"
      contains: "RESCHEDULE"
    - path: apps/web/prisma/migrations/*_add_reschedule_outcome/migration.sql
      provides: "Forward migration that ALTERs the enum and adds the column"
  key_links:
    - from: apps/web/prisma/schema.prisma
      to: apps/web/utils/calendar/reconciliation/persist.ts
      via: "Prisma client regeneration picks up new enum value + column"
      pattern: "rescheduleOfEventId|RESCHEDULE"
---

<objective>
Extend the `ReconciliationOutcome` enum with a new `RESCHEDULE` value, and add a nullable `rescheduleOfEventId` column to `ReconciliationRecord` (D-09, D-10). Generate and commit a Prisma migration so production picks it up via the existing `prisma migrate deploy` step in the prod build.

This is the first wave because every downstream plan (orchestrator emitting `RESCHEDULE`, persist.ts typing, eval fixtures) depends on the enum value existing both in Prisma client types AND in the live database.

Purpose: Without this migration, the orchestrator code in 11-05 will throw a Prisma validation error at runtime when it tries to write `outcome: "RESCHEDULE"`.

Output: Updated schema.prisma + a checked-in migration file + regenerated Prisma client. Local Postgres has the new enum value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-calendar-reconciliation-v2/11-CONTEXT.md
@apps/web/prisma/schema.prisma
@apps/web/utils/calendar/reconciliation/persist.ts
@CLAUDE.md
</context>

<interfaces>
Current enum and model at apps/web/prisma/schema.prisma:898-935 (verbatim):

```prisma
model ReconciliationRecord {
  id        String   @id @default(cuid())
  ...
  outcome             ReconciliationOutcome
  googleEventId       String?
  googleEventHtmlLink String?
  ...
  errorMessage String?
  ...
}

enum ReconciliationOutcome {
  MATCHED
  CREATED
  AMBIGUOUS
  PENDING
  FAILED
}
```

Note: `AMBIGUOUS` is retained for back-compat (Phase 11 will not emit it but legacy rows exist).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add RESCHEDULE enum value + rescheduleOfEventId column to schema.prisma</name>
  <files>apps/web/prisma/schema.prisma</files>
  <read_first>
    - apps/web/prisma/schema.prisma (lines 895-935 — full ReconciliationRecord model and enum)
    - .planning/phases/11-calendar-reconciliation-v2/11-CONTEXT.md (D-09, D-10, and "Claude's Discretion" on column-vs-errorMessage — per D-09 #3 we choose a dedicated column for cleanliness)
  </read_first>
  <action>
    In `apps/web/prisma/schema.prisma`:

    1. In the `ReconciliationOutcome` enum (around line 929), add `RESCHEDULE` on a new line BETWEEN `AMBIGUOUS` and `PENDING`. Preserve all other enum values and ordering. Final order: `MATCHED, CREATED, AMBIGUOUS, RESCHEDULE, PENDING, FAILED`.

    2. In the `ReconciliationRecord` model (around lines 898-927), add a nullable string column `rescheduleOfEventId String?` immediately after the `googleEventHtmlLink String?` line. Add an inline comment `// Phase 11: when outcome=RESCHEDULE, holds the Google event id of the OLD event that this email superseded. The new event id is in googleEventId.`

    No other changes to schema.prisma. Do NOT touch other models. Do NOT add an index on the new column (low cardinality, rarely queried).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path apps/web/prisma/schema.prisma -Pattern 'RESCHEDULE','rescheduleOfEventId' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path apps/web/prisma/schema.prisma -Pattern '^\s+RESCHEDULE$'` returns exactly one match (the enum line).
    - `Select-String -Path apps/web/prisma/schema.prisma -Pattern 'rescheduleOfEventId String\?'` returns exactly one match.
    - The `enum ReconciliationOutcome { ... }` block still contains all five original values: MATCHED, CREATED, AMBIGUOUS, PENDING, FAILED.
    - File line count increased by ~2-3 lines (1 enum line + 1 column line + optional comment).
  </acceptance_criteria>
  <done>Schema file modified, no other lines changed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Run prisma migrate dev to generate and apply migration</name>
  <what-built>Schema changes are written but not yet materialized in the local DB or as a committed migration file. Prod `prisma migrate deploy` (run during the docker build per CLAUDE.md) requires a committed migration file under `apps/web/prisma/migrations/`.</what-built>
  <how-to-verify>
    Run from the `apps/web` directory (NOT from repo root — Prisma resolves DATABASE_URL via apps/web/.env):

    ```
    cd apps/web
    pnpm prisma migrate dev --name add_reschedule_outcome
    ```

    Expected outputs:
    1. A new directory `apps/web/prisma/migrations/<timestamp>_add_reschedule_outcome/` is created with a `migration.sql` file.
    2. The migration SQL should contain two statements:
       - `ALTER TYPE "ReconciliationOutcome" ADD VALUE 'RESCHEDULE';` (or equivalent — Postgres enum value addition)
       - `ALTER TABLE "ReconciliationRecord" ADD COLUMN "rescheduleOfEventId" TEXT;`
    3. Prisma client is regenerated; no TypeScript errors will appear in editor LSP after the next file open.
    4. Local DB has the new enum value (verify by inspecting in `pnpm prisma studio` or via psql — optional).

    If the migration command fails because no local DATABASE_URL is set, the executor should report back and the user will run it on a machine with DB access. Do NOT attempt to run `prisma db push` instead — production uses migration history.
  </how-to-verify>
  <resume-signal>Type "approved" once the migration file is committed-or-staged, OR describe the error encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update persist.ts ReconciliationOutcome type union + accept rescheduleOfEventId in update payload</name>
  <files>apps/web/utils/calendar/reconciliation/persist.ts</files>
  <read_first>
    - apps/web/utils/calendar/reconciliation/persist.ts (current `ReconciliationOutcome` type alias on lines 7-12 and `updateReconciliationRecord` data shape on lines 98-110)
    - .planning/phases/11-calendar-reconciliation-v2/11-CONTEXT.md (D-09 #3 — persist record with outcome=CREATED was the early thinking; D-10 supersedes: store as RESCHEDULE distinctly)
  </read_first>
  <action>
    In `apps/web/utils/calendar/reconciliation/persist.ts`:

    1. Extend the `ReconciliationOutcome` type alias (lines 7-12) to include `"RESCHEDULE"`. Order: `"MATCHED" | "CREATED" | "AMBIGUOUS" | "RESCHEDULE" | "PENDING" | "FAILED"`.

    2. In the `updateReconciliationRecord` function's `data` parameter type (around line 103-108), add an optional field: `rescheduleOfEventId?: string | null;` placed after `googleEventHtmlLink`. The function body needs no change — Prisma's generated update input type will accept the new field once the migration is applied and the client is regenerated.

    Do NOT change `createReconciliationRecord` — RESCHEDULE is set via the update path after the Google API calls succeed, never at row creation (which is always PENDING).

    Do NOT touch the stale-PENDING sweep or any other function.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path apps/web/utils/calendar/reconciliation/persist.ts -Pattern 'RESCHEDULE','rescheduleOfEventId' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path apps/web/utils/calendar/reconciliation/persist.ts -Pattern '"RESCHEDULE"'` returns exactly one match (inside the type alias).
    - `Select-String -Path apps/web/utils/calendar/reconciliation/persist.ts -Pattern 'rescheduleOfEventId\?: string \| null'` returns exactly one match.
    - `createReconciliationRecord` function body is byte-identical to before (no new fields written at create time).
  </acceptance_criteria>
  <done>persist.ts type and update shape extended; no functional logic changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local Postgres | migration SQL runs against local dev DB |
| GitHub Actions build → prod Postgres | `prisma migrate deploy` runs the migration on prod DB at next deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | migration.sql contents | mitigate | migration is autogenerated by `prisma migrate dev` from schema diff; reviewer should diff the generated SQL against expected ALTER TYPE / ALTER TABLE statements before commit |
| T-11-02 | Denial of Service | prod Postgres at deploy | accept | enum-value ADD and nullable-column ADD are both safe additive operations with negligible lock time on a small (<1M rows) reconciliation_record table; Phase 11 enum addition does not require a transaction wrap for Postgres ≥12 |
</threat_model>

<verification>
- schema.prisma diff shows only enum addition + new column + comment.
- `apps/web/prisma/migrations/<timestamp>_add_reschedule_outcome/migration.sql` exists and is committed.
- `persist.ts` ReconciliationOutcome type union includes "RESCHEDULE".
- Editor LSP shows no type errors in persist.ts and (after wave 3) in the orchestrator's RESCHEDULE write path.
</verification>

<success_criteria>
- New migration is committed.
- Schema, migration, and persist.ts type are mutually consistent.
- No other reconciliation files are touched in this plan.
</success_criteria>

<output>
Create `.planning/phases/11-calendar-reconciliation-v2/11-01-SUMMARY.md` when done. Summary should record: migration timestamp, migration SQL contents (truncated to the ALTER statements), and any deviations.
</output>
