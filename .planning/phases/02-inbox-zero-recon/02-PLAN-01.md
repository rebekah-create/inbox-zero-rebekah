---
phase: 02-inbox-zero-recon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md
autonomous: true
requirements:
  - RECON-01
  - RECON-02
  - RECON-03
  - RECON-04
  - RECON-05
  - RECON-06

must_haves:
  truths:
    - "RECON.md exists at .planning/phases/02-inbox-zero-recon/RECON.md"
    - "RECON.md documents the classification pipeline with entry point, inputs, matching logic, AI prompt structure, and outputs"
    - "RECON.md documents the rules engine with storage schema, evaluation order, and application steps"
    - "RECON.md documents all AI call sites with model slot, function name, and purpose"
    - "RECON.md documents all six database tables relevant to classification and digests"
    - "RECON.md has a keep/replace/extend decision for each of six named components with rationale"
    - "RECON.md has a cost analysis table comparing current (~$7.26/month) vs. proposed three-tier (~$1.88/month)"
    - "RECON.md answers the three open questions from RESEARCH.md using live production data"
  artifacts:
    - path: "C:\\Users\\rebek\\Documents\\inbox-zero-rebekah\\.planning\\phases\\02-inbox-zero-recon\\RECON.md"
      provides: "Complete hand-off document for Phase 3"
      min_lines: 300
      contains:
        - "## Classification Pipeline"
        - "## Rules Engine"
        - "## AI Integration"
        - "## Database Schema"
        - "## Component Decisions"
        - "## Cost Analysis"
        - "## Open Questions"
  key_links:
    - from: "RECON.md ## Component Decisions"
      to: "RECON.md ## Classification Pipeline"
      via: "Decision references specific pipeline behavior it replaces or extends"
    - from: "RECON.md ## Cost Analysis"
      to: "RECON.md ## AI Integration"
      via: "Cost numbers reference model slots documented in AI Integration section"
---

<objective>
Write RECON.md — the complete hand-off document from Phase 2 to Phase 3.

Purpose: Phase 3 (Classification Engine) cannot start until every major Inbox Zero component is mapped with a keep/replace/extend decision. RECON.md is the authoritative reference that prevents Phase 3 from building on wrong assumptions about what already exists.

Output: A single file — `C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md` — covering all six RECON requirements.
</objective>

<execution_context>
@C:\Users\rebek\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\rebek\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\ROADMAP.md
@C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\STATE.md
@C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RECON.md structural and technical sections (RECON-01 through RECON-04)</name>

  <read_first>
    - C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\02-RESEARCH.md — primary source for ALL content; read in full before writing a single line
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\app\api\google\webhook\route.ts — confirm webhook entry flow
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\ai\choose-rule\run-rules.ts — confirm rule orchestration flow
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\ai\choose-rule\match-rules.ts — confirm matching logic order
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\ai\choose-rule\ai-choose-rule.ts — confirm AI prompt structure
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\llms\model.ts — confirm model slot definitions and fallback behavior
    - C:\Users\rebek\Documents\inbox-zero-rebekah\prisma\schema.prisma — confirm all table definitions (Rule, Action, ExecutedRule, ExecutedAction, Group, GroupItem, ClassificationFeedback, Newsletter, Digest, DigestItem, Schedule)
  </read_first>

  <files>C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md</files>

  <action>
Create RECON.md at `C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md`.

Read RESEARCH.md in full first. Then read each source file listed in read_first to verify accuracy before writing. Cross-check every claim against the actual source code.

Write the following sections in order. Use the exact section headers shown — Phase 3 plans will reference these headers.

---

## File header

```
# RECON.md — Inbox Zero Fork: Component Map and Decisions
Phase: 2 — Inbox Zero Recon
Date: [today's date]
Produced by: Phase 2 execution
Hand-off to: Phase 3 — Classification Engine

This document maps every major component of the Inbox Zero fork relevant to
classification and digests. Each component has a keep/replace/extend decision
with rationale. Phase 3 MUST read this document before writing any code.
```

---

## Section: Classification Pipeline (RECON-01)

Write the following subsections. Use content from RESEARCH.md "Classification Pipeline" section, verified against source files:

**Subsection: Entry Point**
Write the full call chain from PubSub delivery to `runRules()`. Include every function name and file path in the chain. Match RESEARCH.md exactly:
```
Google PubSub → POST /api/google/webhook
  → token verification (GOOGLE_PUBSUB_VERIFICATION_TOKEN)
  → getWebhookEmailAccount()
  → rate-limit check
  → after() — defers to background
    → processHistoryForUser()
      → getHistory()
      → processHistoryItem() for each history item
        → LABEL_REMOVED → handleLabelRemovedEvent() → saveClassificationFeedback()
        → LABEL_ADDED → handleLabelAddedEvent() → saveClassificationFeedback()
        → MESSAGE_ADDED → markMessageAsProcessing() → processHistoryItemShared()
          → fetchMessage() → runRules()
```
List the five key files with their roles.

**Subsection: Inputs to Classification**
List every field of `ParsedMessage` used during classification: `headers.from`, `headers.to`, `headers.subject`, `headers.date`, `textPlain`, `listUnsubscribe`, `threadId`, `messageId`, `internalDate`.

**Subsection: Matching Logic (ordered)**
Write the full ordered matching sequence — exactly four steps:
1. Cold email check (`isColdEmail()`) — called before all other rules; returns immediately if cold
2. For each remaining rule: (a) learned patterns via `matchesGroupRule()` — checks GroupItem rows; short-circuits if matched; (b) static conditions via `matchesStaticRule()` — regex on from/to/subject/body fields; supports `*` wildcards and pipe/comma OR syntax; (c) AND/OR logic per rule via `conditionalOperator`
3. AI call — only if `potentialAiMatches.length > 0` AND no learned pattern already matched; calls `aiChooseRule()`
4. Conversation tracking — separate meta-rule from `systemType` conversation rules; resolved after main matching

**Subsection: AI Prompt Structure**
Write out the exact prompt structure for single-rule mode (default). Include:
- SYSTEM: what each part contains (instructions block, guidelines block, `getUserRulesPrompt()`, `formatClassificationFeedback()` up to 10 items, `getUserInfoPrompt()`)
- PROMPT: the email content wrapped in `<email>` tags with `stringifyEmail(email, 500)`
- SCHEMA (Zod): `{ reasoning: string, ruleName: string | null, noMatchFound: boolean }`
- Note multi-rule mode schema: `matchedRules: [{ ruleName, isPrimary }][]`

**Subsection: Confidence Scoring — GAP**
State explicitly: "There is NO numeric confidence score in the current codebase. `ExecutedRule.reason` stores the AI's text reasoning. `ExecutedRule.matchMetadata` stores `MatchReason[]` with types STATIC, AI, LEARNED_PATTERN, PRESET — no numeric score. Phase 3 MUST add `confidenceScore Float?` to `ExecutedRule` via Prisma migration."

**Subsection: Outputs**
List the four output steps:
1. `getActionItemsWithAiArgs()` — second AI call to fill template variables (only when needed)
2. `executeAct()` — Gmail API calls
3. `prisma.executedRule.create()` — writes ExecutedRule + ExecutedAction
4. For `ActionType.DIGEST` actions → `enqueueDigestItem()` → writes DigestItem

---

## Section: Rules Engine (RECON-02)

**Subsection: Storage**
Document all three tables. For each, list every column with its type and role. Use the exact schema from RESEARCH.md, verified against prisma/schema.prisma:

Rule table: `id`, `name`, `enabled`, `emailAccountId`, `instructions`, `from`, `to`, `subject`, `body`, `conditionalOperator`, `groupId`, `systemType`, `runOnThreads`, `promptText`

Action table: `type` (ActionType enum — list all values: ARCHIVE, LABEL, REPLY, SEND_EMAIL, FORWARD, DRAFT_EMAIL, DIGEST, MOVE_FOLDER, MARK_SPAM, MARK_READ, CALL_WEBHOOK), `label`, `labelId`, `subject`, `content`, `to`, `cc`, `bcc`, `delayInMinutes`, `ruleId`

Group/GroupItem tables: GroupItem columns: `type` (FROM/SUBJECT/BODY), `value`, `exclude`, `source` (AI/USER/LABEL_REMOVED/LABEL_ADDED)

**Subsection: Evaluation**
Document the five-step evaluation in `match-rules.ts` / `findPotentialMatchingRules()`:
1. For rules with groupId: lazy-load all Groups, then `findMatchingGroup()` — O(n) scan of GroupItems
2. Static evaluation: `matchesStaticRule()` — regex on message fields
3. AND/OR logic via `conditionalOperator`
4. If only AI conditions remain → rule goes to `potentialAiMatches[]`
5. If learned pattern match found → skip all potentialAiMatches (optimization)

**Subsection: Application**
Document the five-step application in `executeMatchedRule()`:
1. `getActionItemsWithAiArgs()` — fills template variables
2. Split into `immediateActions` and `delayedActions` by `delayInMinutes`
3. Write `ExecutedRule` + `ExecutedAction` records atomically
4. `executeAct()` for immediate actions
5. `scheduleDelayedActions()` via BullMQ for delayed actions

---

## Section: AI Integration (RECON-03)

**Subsection: Model Tier Slots**
Write a table with five rows (one per slot): default, economy, nano, chat, draft. For each row: slot name, env vars pair (`*_LLM_PROVIDER` / `*_LLM_MODEL`), current fork value (from RESEARCH.md — economy/nano/chat/draft are UNSET), fallback behavior.

State explicitly: "In the current production fork: `DEFAULT_LLM_PROVIDER=anthropic` resolves to `claude-sonnet-4-6`. No `ECONOMY_LLM_*` vars are set — economy tasks fall back to Sonnet. This is the primary cost problem Phase 3 must fix."

**Subsection: AI Call Sites**
Write a table of all classification-relevant AI call sites. Six rows minimum:
| File | Function | Model slot | Purpose |
|------|----------|-----------|---------|
(Use exact data from RESEARCH.md AI Integration section.)

**Subsection: SDK and Prompt Hardening**
Document `createGenerateObject()` from `utils/llms/index.ts`: applies prompt hardening (`promptHardening: { trust: "untrusted", level: "full" | "compact" }`), uses Vercel AI SDK `generateObject()` with Zod schemas, logs token usage.

**Subsection: No Discrete Classification Endpoint**
State explicitly: "No dedicated `/api/ai/classify` endpoint exists. Classification happens inline during webhook processing via `after()`. The `apps/web/app/api/ai/` directory has specialized endpoints but classification is NOT a discrete HTTP endpoint — it is embedded in the webhook flow."

---

## Section: Database Schema (RECON-04)

**Subsection: Classification Tables**
Write a table for each of these eight tables with columns and role column:
- Rule
- Action
- ExecutedRule
- ExecutedAction
- Group
- GroupItem
- ClassificationFeedback
- Newsletter

For ExecutedRule, include the `matchMetadata` JSON structure:
```typescript
// MatchReason[]
type MatchReason =
  | { type: "STATIC" }
  | { type: "AI" }
  | { type: "PRESET"; systemType: SystemType }
  | { type: "LEARNED_PATTERN"; groupItem: GroupItem; group: Group }
```

Note explicitly: "No `confidenceScore` column exists. Phase 3 must add it."

**Subsection: Digest Tables**
Write a table for Digest, DigestItem, and Schedule with columns and role.

For DigestItem, include the content structure:
```typescript
// storedDigestContentSchema
{ content: string }  // AI-summarized text; redacted to "[REDACTED]" after send
```

Note: "DigestItem.content is redacted to `[REDACTED]` after digest send. Feedback links in the digest email must encode sufficient state in the URL token to identify the email without re-reading DigestItem.content."
  </action>

  <verify>
    <automated>grep -c "## Classification Pipeline" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "## Rules Engine" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "## AI Integration" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "## Database Schema" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "confidenceScore" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
  </verify>

  <acceptance_criteria>
    - RECON.md exists at the output path
    - `grep -c "## Classification Pipeline" RECON.md` returns 1
    - `grep -c "## Rules Engine" RECON.md` returns 1
    - `grep -c "## AI Integration" RECON.md` returns 1
    - `grep -c "## Database Schema" RECON.md` returns 1
    - RECON.md contains the string "NO numeric confidence score"
    - RECON.md contains the string "confidenceScore Float?"
    - RECON.md contains the string "ECONOMY_LLM_"
    - RECON.md contains the string "DigestItem.content"
    - RECON.md contains the string "ActionType" and "DIGEST"
    - RECON.md word count exceeds 1000 words (Task 1 alone; full file will be larger after Task 2)
  </acceptance_criteria>

  <done>RECON.md exists containing all four technical sections with the exact section headers shown. Classification pipeline entry chain, inputs, matching logic, prompt structure, confidence gap, and outputs are all documented. Rules engine storage/evaluation/application documented with full column lists. AI integration model tiers and call sites documented in tables. Database schema documented for all eight classification tables and three digest tables.</done>
</task>

<task type="auto">
  <name>Task 2: Write component decisions, cost analysis, and answer open questions (RECON-05, RECON-06)</name>

  <read_first>
    - C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\02-RESEARCH.md — Component Decisions and Cost Analysis sections; read before writing
    - C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md — read current state of file written by Task 1 before appending
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\ai\choose-rule\ai-choose-rule.ts — verify Decision 3 claims about model selection
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\utils\ai\actions.ts — verify Decision 4 claims about DIGEST action
    - C:\Users\rebek\Documents\inbox-zero-rebekah\apps\web\app\api\resend\digest\route.ts — verify Decision 5 claims about digest pipeline
  </read_first>

  <files>C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md</files>

  <action>
Append three sections to RECON.md. Read the file first to see what Task 1 already wrote, then append. Do NOT rewrite what Task 1 produced.

---

## Section: Component Decisions (RECON-05)

Write six decisions. Each decision has:
- Decision number and component name as a heading (e.g., `### Decision 1: Webhook Entry Point`)
- A "Decision:" line stating exactly one of: KEEP, KEEP + EXTEND, REPLACE (model selection), KEEP (prompt structure)
- A "Rationale:" paragraph explaining WHY
- A "Phase 3 action:" line stating exactly what Phase 3 must do as a result (or "None — keep as-is")

Decision 1: Webhook Entry Point (`/api/google/webhook`)
- Decision: KEEP
- Rationale: Token verification, rate-limit guard, and `after()` deferral pattern are correct and production-ready. Provider-agnostic (handles both Gmail and Outlook paths). No changes needed for the classification engine.
- Phase 3 action: None — keep as-is.

Decision 2: `match-rules.ts` — Static and Learned Pattern Matching
- Decision: KEEP + EXTEND
- Rationale: Static regex matching and learned-pattern (Group) short-circuit are sound and cost-free. GroupItem matching already functions as a free fast path equivalent to Tier 1 of the proposed three-tier architecture. The extension needed is explicit priority ordering so user-defined explicit rules from the new Rules UI are evaluated as the highest-priority tier before learned patterns.
- Phase 3 action: Add explicit priority ordering — user-created explicit rules run before system/learned rules. No replacement of the matching logic itself.

Decision 3: `ai-choose-rule.ts` — LLM Rule Selection
- Decision: REPLACE model selection; KEEP prompt structure
- Rationale: The prompt structure (system instructions + rules list + classification feedback + email content → structured JSON output) is well-designed. What must change: replace `getModel(emailAccount.user, "default")` with tiered escalation — call Haiku first; escalate to Sonnet only if Haiku returns low confidence or `noMatchFound = true`. Also: add `confidenceScore: number` field to the Zod response schema so Phase 3 can persist it.
- Phase 3 action: (1) Change model call from `getModel(..., "default")` to `getModel(..., "economy")` for Haiku tier. (2) Add `confidenceScore: z.number().min(0).max(1)` to the Zod schema. (3) On `noMatchFound = true` or `confidenceScore < threshold`, re-call with `getModel(..., "default")` (Sonnet). (4) Set `ECONOMY_LLM_PROVIDER=anthropic` and `ECONOMY_LLM_MODEL=claude-haiku-3-5` in SSM.

Decision 4: `actions.ts` — DIGEST Action Type
- Decision: KEEP + EXTEND
- Rationale: The `DIGEST` action type and `enqueueDigestItem()` infrastructure are correct. Digest is opt-in per rule: a rule must have an `Action` row of type `DIGEST` for its matched emails to appear in the morning summary. This is not automatic.
- Phase 3 action: Ensure each of the eight classification rules (Receipts, Deals, Newsletters, Marketing, Urgent, 2FA, Uncertain, Greers List) has the correct actions attached: LABEL + ARCHIVE + DIGEST for most; LABEL + DIGEST (no ARCHIVE) for Urgent and Uncertain; DELETE after 24h for 2FA via `delayInMinutes`.

Decision 5: Digest Send Pipeline (`/api/resend/digest`, `send-digest.ts`)
- Decision: KEEP + EXTEND
- Rationale: React Email + Resend rendering, Schedule table, and PENDING→PROCESSING→SENT state machine are correct and operational. The pipeline is already sending digests (Phase 1 fixed the from-address).
- Phase 3 / Phase 4 action: (1) Add thumbs-up/down feedback links per DigestItem (DIGEST-07) — links must encode email identity in URL token since DigestItem.content is redacted post-send. (2) Add explicit Urgent and Uncertain sections to the digest template. (3) Verify `Schedule.timeOfDay` is set to deliver between 6-7am Eastern.

Decision 6: `ClassificationFeedback` + Label-Change Learning
- Decision: KEEP + EXTEND
- Rationale: The label-add/remove learning loop is live and already feeding back into AI prompts (up to 10 items per sender). This is a strong foundation for Phase 6. No replacement needed.
- Phase 6 action: Add thumbs-up/down feedback from digest email. This requires either a new `eventType` value or a companion table since digest votes are explicit (user-initiated) rather than implicit (label changes observed by webhook).

---

## Section: Cost Analysis (RECON-06)

Write a cost analysis with the following exact structure. Copy numbers from RESEARCH.md Cost Analysis section. Mark all estimates clearly as [ASSUMED] where the source file marks them as assumed.

**Subsection: Current Architecture Cost Estimate**

State inputs:
- Single user: rebekah@trueocean.com
- Estimated email volume: 20-50 emails/day [ASSUMED]
- Model today: claude-sonnet-4-6 for ALL calls (default AND economy, because ECONOMY_LLM_* unset)
- Pricing: claude-sonnet-4-6 at $3.00/MTok input, $15.00/MTok output [ASSUMED — verify at anthropic.com/pricing]

Write the per-email calculation:
- aiChooseRule(): ~500-800 tokens input, ~50-100 tokens output
- getActionItemsWithAiArgs(): ~300-500 tokens input, ~50 tokens output (when template vars needed)
- Average: 1.5 AI calls per email

Monthly estimate (35 emails/day average):
- Classification: (0.945 MTok × $3.00) + (0.105 MTok × $15.00) = $2.84 + $1.58 = ~$4.42/month
- Digest summarization (also Sonnet due to unset economy vars): 1,050 summaries × 500 tokens = (0.42 × $3.00) + (0.105 × $15.00) = $1.26 + $1.58 = ~$2.84/month
- **Total current estimate: ~$7.26/month** [ALL NUMBERS ASSUMED — not measured]

**Subsection: Proposed Three-Tier Architecture Cost**

Tier 1 (explicit rules — free): Estimated 60-70% of emails matched by static rules or learned GroupItems → zero AI cost. At 35 emails/day, ~22 caught free, ~13 reach AI.

Tier 2 (Haiku for uncertain): claude-haiku-3-5 at $0.80/MTok input, $4.00/MTok output [ASSUMED].
- 13 emails/day × 30 days = 390 emails/month
- Cost: (0.351 × $0.80) + (0.039 × $4.00) = $0.28 + $0.16 = **~$0.44/month**

Tier 3 (Sonnet for hard cases): ~10-15% of Haiku cases escalate (~45 emails/month).
- Cost: (0.0405 × $3.00) + (0.0045 × $15.00) = $0.12 + $0.07 = **~$0.19/month**

Digest summarization (move to Haiku): 1,050 summaries × 500 tokens.
- Cost: (0.525 × $0.80) + (0.105 × $4.00) = $0.42 + $0.42 = **~$0.84/month**

Daily digest narrative (Sonnet, once/day): 30 calls/month × ~2,000 input, ~500 output.
- Cost: (0.06 × $3.00) + (0.015 × $15.00) = $0.18 + $0.23 = **~$0.41/month**

**Total proposed estimate: ~$1.88/month** [ALL NUMBERS ASSUMED]

Write a summary table:
| Scenario | Monthly AI Cost | Savings |
|----------|----------------|---------|
| Current (all Sonnet) | ~$7.26 | — |
| Proposed three-tier | ~$1.88 | ~$5.38/month (74%) |
| Budget ceiling | $10.00 additional | Well under ceiling |

Add the key uncertainty note: "Email volume and tier-1 hit rate are estimated, not measured. Revisit after Phase 3 is live and real token counts are available from Anthropic's usage dashboard."

---

## Section: Open Questions — Live Production Answers

For each of the three open questions from RESEARCH.md, answer them by running the specified commands against the production environment. Document what command was run and what was found.

**Question 1: Is the Anthropic API key pay-as-you-go or prepaid credits?**
- Run: Check Anthropic console or ask Rebekah
- Record the actual answer found, or "Unable to determine — verify manually at console.anthropic.com before Phase 3"

**Question 2: How many Rules currently exist for rebekah@trueocean.com?**
- Run this SSH command against the production server: `ssh ec2-user@<production-host> 'docker exec <postgres-container> psql -U inbox_zero -d inbox_zero -c "SELECT count(*) FROM \"Rule\" WHERE \"emailAccountId\" IN (SELECT id FROM \"EmailAccount\" WHERE \"userId\" IN (SELECT id FROM \"User\" WHERE email = '"'"'rebekah@trueocean.com'"'"'))"'`
- Record the count. If SSH is not available in this context, state "Query not run — execute manually on production server before Phase 3 planning" and note the exact SQL to run.

**Question 3: Is ECONOMY_LLM_PROVIDER set in production SSM?**
- Run: `aws ssm get-parameter --name /inbox-zero/ECONOMY_LLM_PROVIDER --region us-east-1 2>&1`
- Record the output. If the command returns a ParameterNotFound error, state that explicitly. If it returns a value, record the value.

---

## Section: Phase 3 Prerequisites Checklist

Write a checklist of concrete things Phase 3 must do before writing classification code. Derive this from the decisions and pitfalls above:

- [ ] Add `confidenceScore Float?` to `ExecutedRule` in prisma/schema.prisma and run migration
- [ ] Set `ECONOMY_LLM_PROVIDER=anthropic` and `ECONOMY_LLM_MODEL=claude-haiku-3-5` in SSM
- [ ] Set `NANO_LLM_PROVIDER=anthropic` and `NANO_LLM_MODEL=claude-haiku-3-5` in SSM
- [ ] Create the eight classification Rules in the database (or seed script) with correct Action rows
- [ ] Verify Anthropic API key spending limit is not set below $10/month
- [ ] Confirm `multiRuleSelectionEnabled = false` is correct for single-rule eight-category taxonomy
  </action>

  <verify>
    <automated>grep -c "## Component Decisions" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "## Cost Analysis" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "## Open Questions" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "KEEP\|EXTEND\|REPLACE" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
    <automated>grep -c "7.26\|1.88" "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "## Component Decisions" RECON.md` returns 1
    - `grep -c "## Cost Analysis" RECON.md` returns 1
    - `grep -c "## Open Questions" RECON.md` returns 1
    - `grep -c "## Phase 3 Prerequisites" RECON.md` returns 1
    - RECON.md contains "Decision: KEEP" at least once
    - RECON.md contains "KEEP + EXTEND" at least twice
    - RECON.md contains "REPLACE" at least once
    - RECON.md contains "7.26" (current cost figure)
    - RECON.md contains "1.88" (proposed cost figure)
    - RECON.md contains "Phase 3 action:" at least six times (one per decision)
    - RECON.md contains "confidenceScore Float?" in prerequisites checklist
    - RECON.md contains "ECONOMY_LLM_PROVIDER" in prerequisites checklist
    - Open Questions section contains either an actual answer or "Unable to determine" for each of the three questions
    - RECON.md total word count exceeds 2000 words
  </acceptance_criteria>

  <done>RECON.md contains all six sections. Component Decisions section has six named decisions each with decision/rationale/Phase 3 action. Cost Analysis section has current (~$7.26/month) and proposed (~$1.88/month) breakdowns with a summary table. Open Questions section documents the result of checking each of the three questions against production. Phase 3 Prerequisites checklist is present with six actionable items. All RECON requirements are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → production SSH | If Task 2 executor runs SSH to query Rule count, it touches the production database in read-only mode |
| Executor → AWS SSM | `aws ssm get-parameter` reads a secret from production SSM — read-only, no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | aws ssm get-parameter in Task 2 | accept | Read-only parameter lookup; ECONOMY_LLM_PROVIDER is not a sensitive secret (it is a provider name, not a credential). API keys are stored under different parameter names. |
| T-02-02 | Information Disclosure | SSH → production DB query in Task 2 | accept | Read-only COUNT query; no PII returned (count only). If SSH is not available in executor context, query is deferred to manual execution. |
| T-02-03 | Tampering | RECON.md written to planning directory | accept | File is a planning artifact in a non-public directory. No production code is modified in this phase. |
| T-02-04 | Denial of Service | Executor reads large source files (prisma/schema.prisma, run-rules.ts) | accept | Read-only file access; no resource exhaustion risk. Files are < 2000 lines each. |
</threat_model>

<verification>
After both tasks complete, verify:

1. RECON.md exists: `ls -la "C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md"`
2. All six section headers present:
   - `grep "^## " RECON.md` must return: Classification Pipeline, Rules Engine, AI Integration, Database Schema, Component Decisions, Cost Analysis, Open Questions, Phase 3 Prerequisites
3. Six decisions present: `grep -c "Decision: KEEP\|Decision: REPLACE" RECON.md` returns 6 or more
4. Cost figures present: `grep "7.26\|1.88" RECON.md` returns matches
5. Confidence score gap documented: `grep "confidenceScore Float" RECON.md` returns a match
6. All six RECON requirements addressed:
   - RECON-01: Classification Pipeline section present with entry chain, inputs, matching logic, prompt, outputs
   - RECON-02: Rules Engine section present with Rule/Action/Group tables and evaluation/application steps
   - RECON-03: AI Integration section present with model tiers table and call sites table
   - RECON-04: Database Schema section present with 8 classification tables + 3 digest tables
   - RECON-05: Component Decisions section present with 6 decisions
   - RECON-06: Cost Analysis section present with current vs. proposed table
</verification>

<success_criteria>
RECON.md exists at `C:\Users\rebek\Documents\inbox-zero-rebekah\.planning\phases\02-inbox-zero-recon\RECON.md` containing:
- Eight top-level sections (six required + Open Questions + Phase 3 Prerequisites)
- Classification pipeline documented from PubSub entry to ExecutedRule write
- Confidence score gap explicitly flagged with the migration action required
- Rules engine documented with all three storage tables, evaluation order, and application steps
- All five model tier slots documented; current production state (economy = Sonnet) called out
- All six classification-relevant AI call sites in a table
- All eleven database tables documented (8 classification + 3 digest)
- Six keep/replace/extend decisions with rationale and Phase 3 action per decision
- Cost comparison: ~$7.26/month current vs. ~$1.88/month proposed (74% savings)
- Three open questions answered or deferred with specific follow-up instructions
- Phase 3 prerequisites checklist with six actionable items
</success_criteria>

<output>
After completion, create `.planning/phases/02-inbox-zero-recon/02-01-SUMMARY.md` using the summary template at `C:\Users\rebek\.claude\get-shit-done\templates\summary.md`.
</output>
