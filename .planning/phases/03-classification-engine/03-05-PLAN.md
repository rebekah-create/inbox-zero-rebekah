---
phase: 03-classification-engine
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [CLASS-01, CLASS-02, CLASS-03, CLASS-04, CLASS-05, CLASS-06, CLASS-07, CLASS-08]

must_haves:
  truths:
    - "A test email arriving via Gmail PubSub is classified into one of the 8 categories within 2 minutes (CLASS-08)"
    - "ExecutedRule rows show non-null confidenceScore for AI-tier classifications (CLASS-03)"
    - "ExecutedRule rows show null confidenceScore for static (Greers List) and learned-pattern matches (CLASS-03 semantics)"
    - "An email from greers@trueocean.com classifies via static rule with no AI call in logs (CLASS-06)"
    - "Urgent and Uncertain test emails remain in inbox (no ARCHIVE action fired) (CLASS-04)"
    - "Receipts/Deals/Newsletters/Marketing test emails are archived (CLASS-04)"
    - "A 2FA-pattern email creates a ScheduledAction row with type=DELETE and scheduledAt ~24h in future (CLASS-05)"
    - "A DigestItem row is created for each Receipts/Deals/Newsletters/Urgent/Uncertain test email"
    - "Logs show 'Choose rule (economy)' label for low-cost cases and 'Choose rule (escalated)' only for ambiguous ones"
  artifacts: []
  key_links:
    - from: "Live Gmail email"
      to: "ExecutedRule + ExecutedAction + DigestItem (where applicable) + ScheduledAction (for 2FA)"
      via: "PubSub -> webhook -> runRules -> aiChooseRule -> executeMatchedRule"
      pattern: "end-to-end production trace"
---

<objective>
Wave 4 — end-to-end manual verification on the live production system. Confirm all 8 CLASS requirements behave as designed by sending real emails and inspecting the resulting DB rows and Gmail state.

Purpose: Code unit tests prove logic; this plan proves the deployed system actually classifies real email correctly within 2 minutes. The manual-only items (CLASS-04, CLASS-05, CLASS-08) cannot be verified any other way.

Output: A verification report capturing observed behavior for every CLASS-XX requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-classification-engine/03-CONTEXT.md
@.planning/phases/03-classification-engine/03-VALIDATION.md
@.planning/phases/03-classification-engine/03-RESEARCH.md
@.planning/phases/03-classification-engine/03-01-SUMMARY.md
@.planning/phases/03-classification-engine/03-03-SUMMARY.md
@.planning/phases/03-classification-engine/03-04-SUMMARY.md

<interfaces>
<!-- Production access (verified RECON Q2): -->
<!-- ssh -i ~/.ssh/inbox_key ubuntu@inbox.tdfurn.com -->
<!-- docker compose logs -f app    — live tail -->
<!-- docker compose exec app psql  — DB inspection -->

<!-- Tables to inspect: -->
<!-- ExecutedRule (id, threadId, messageId, ruleId, confidenceScore, reason, matchMetadata, createdAt) -->
<!-- ExecutedAction (executedRuleId, type, label, ...) -->
<!-- ScheduledAction (executedRuleId, type, scheduledAt, status) -->
<!-- DigestItem (messageId, threadId, content, digestId, actionId) -->

<!-- Test email patterns to send (sender accounts the operator controls): -->
<!-- T1: A normal newsletter — should classify as Newsletters (LABEL+ARCHIVE+DIGEST) -->
<!-- T2: An order confirmation from any retailer — Receipts -->
<!-- T3: A deal/promotion email — Deals -->
<!-- T4: A generic marketing email — Marketing -->
<!-- T5: An urgent account-security or time-sensitive email — Urgent (LABEL+DIGEST, NO archive) -->
<!-- T6: A 2FA / OTP email pattern — 2FA (LABEL+DELETE@1440min) -->
<!-- T7: A genuinely ambiguous email — Uncertain -->
<!-- T8: An email from greers@trueocean.com — Greers List (Tier 1 static, no AI) -->

<!-- Two-minute SLA timer (CLASS-08): from Gmail "Sent" timestamp on the test message to ExecutedRule.createdAt -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Send test emails T1-T8 and verify ExecutedRule + Action behavior</name>
  <read_first>
    - .planning/phases/03-classification-engine/03-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/03-classification-engine/03-CONTEXT.md (D-05 — exact actions per rule)
    - .planning/phases/03-classification-engine/03-04-SUMMARY.md (deploy SHA — confirm prod is on the right build)
  </read_first>
  <what-built>
    Operator sends 8 test emails to rebekah@trueocean.com, one per category, and inspects the resulting DB state.

    Setup (do once before sending any test emails):

    1. Open a tail on production logs in one terminal:
       ```bash
       ssh -i ~/.ssh/inbox_key ubuntu@inbox.tdfurn.com 'docker compose logs -f --tail=0 app' | tee /tmp/phase3-uat.log
       ```

    2. Open a psql shell in another terminal:
       ```bash
       ssh -i ~/.ssh/inbox_key ubuntu@inbox.tdfurn.com 'docker compose exec app psql -U inbox_zero -d inbox_zero'
       ```

    3. Note the current max(ExecutedRule.id) or createdAt cutoff so post-test queries can scope to new rows:
       ```sql
       SELECT max("createdAt") FROM "ExecutedRule";
       ```
       Record this value as `T_START`.

    Send the 8 test emails (one at a time, ~1 minute apart so logs are easy to correlate):

    For each email, capture:
    - The Gmail "Sent" timestamp (visible in the sender's "Sent" folder).
    - The Gmail messageId (visible in the message source / headers as `Message-ID:`).
    - The expected category.

    | # | From | Subject pattern | Body sketch | Expected rule | Expected actions |
    |---|------|-----------------|-------------|---------------|------------------|
    | T1 | a real newsletter you're subscribed to | "[Newsletter] ..." | List-Unsubscribe header present | Newsletters | LABEL,ARCHIVE,DIGEST |
    | T2 | order@somestore.com (or retailer) | "Your order #12345 is confirmed" | Receipt-style content | Receipts | LABEL,ARCHIVE,DIGEST |
    | T3 | deals@somestore.com | "30% off this weekend only" | Discount/sale wording | Deals | LABEL,ARCHIVE,DIGEST |
    | T4 | brand@example.com | "Introducing our new line" | Brand promo, no specific deal | Marketing | LABEL,ARCHIVE (no DIGEST) |
    | T5 | a real person | "URGENT: payment failure on account" | Time-sensitive, action required | Urgent | LABEL,DIGEST (NO ARCHIVE) |
    | T6 | noreply@github.com or similar | "Your verification code is 123456" | OTP body | 2FA | LABEL,DELETE+1440min |
    | T7 | mixed/unclear sender | ambiguous content | genuinely doesn't fit | Uncertain | LABEL,DIGEST (NO ARCHIVE) |
    | T8 | greers@trueocean.com | any subject | any body | Greers List | LABEL,ARCHIVE (Tier 1 static) |

    Wait 3 minutes after the last email, then run the verification queries below.
  </what-built>
  <how-to-verify>
    Run these psql queries (copy-paste into the psql shell from setup step 2). Capture output for the SUMMARY.

    Query A — All 8 ExecutedRule rows since T_START:
    ```sql
    SELECT
      er.id,
      er."messageId",
      er."createdAt",
      r.name AS rule_name,
      er."confidenceScore",
      er."matchMetadata",
      er.reason
    FROM "ExecutedRule" er
    LEFT JOIN "Rule" r ON r.id = er."ruleId"
    WHERE er."createdAt" > '<T_START>'
    ORDER BY er."createdAt";
    ```

    Verify:
    - Exactly 8 rows (one per test email; if any test email produced multiple rule matches, investigate — D-07 locks single match).
    - rule_name matches the expected column for each test email.
    - confidenceScore is non-null for T1-T7 (AI tier was used).
    - confidenceScore IS NULL for T8 (Greers List static match — no AI call) — verify matchMetadata for T8 contains a STATIC type entry.

    Query B — Action rows fired per ExecutedRule:
    ```sql
    SELECT
      er.id,
      r.name AS rule_name,
      array_agg(ea.type ORDER BY ea.type) AS action_types
    FROM "ExecutedRule" er
    JOIN "Rule" r ON r.id = er."ruleId"
    LEFT JOIN "ExecutedAction" ea ON ea."executedRuleId" = er.id
    WHERE er."createdAt" > '<T_START>'
    GROUP BY er.id, r.name
    ORDER BY er."createdAt";
    ```

    Verify per-row action sets match D-05:
    - Receipts, Deals, Newsletters: {ARCHIVE, DIGEST, LABEL}
    - Marketing: {ARCHIVE, LABEL}
    - Urgent, Uncertain: {DIGEST, LABEL}     (NO ARCHIVE)
    - 2FA: {DELETE, LABEL}                    (DELETE has delayInMinutes=1440 — confirmed via Query D)
    - Greers List: {ARCHIVE, LABEL}

    Query C — Tier usage from logs (CLASS-02):
    ```bash
    grep -E 'Choose rule \((economy|escalated)\)' /tmp/phase3-uat.log | wc -l
    grep -F 'Choose rule (economy)' /tmp/phase3-uat.log | wc -l
    grep -F 'Choose rule (escalated)' /tmp/phase3-uat.log | wc -l
    ```

    Verify:
    - economy count >= 7 (T1-T7 all use Haiku at minimum). T8 should NOT appear (static rule, no AI).
    - escalated count is small (only the genuinely ambiguous T7 likely escalates; ideally T1-T6 stay on Haiku).
    - No "falling back to default" warning lines in /tmp/phase3-uat.log.

    Query D — 2FA scheduled DELETE (CLASS-05):
    ```sql
    SELECT
      sa.id,
      sa.type,
      sa."scheduledAt",
      sa.status,
      EXTRACT(EPOCH FROM (sa."scheduledAt" - NOW()))/3600 AS hours_until
    FROM "ScheduledAction" sa
    WHERE sa."executedRuleId" IN (
      SELECT id FROM "ExecutedRule"
      WHERE "messageId" = '<T6_messageId>'
    );
    ```

    Verify:
    - Exactly 1 row.
    - type = 'DELETE'.
    - hours_until between 23.5 and 24.5 (i.e., 1440 minutes ± small drift).
    - status = 'PENDING' (or whatever the initial enum value is in the codebase).

    Query E — DigestItem rows (CLASS-03 supporting data + Phase 4 prep):
    ```sql
    SELECT
      di.id,
      di."messageId",
      r.name AS rule_name
    FROM "DigestItem" di
    JOIN "ExecutedAction" ea ON ea.id = di."actionId"
    JOIN "ExecutedRule" er ON er.id = ea."executedRuleId"
    JOIN "Rule" r ON r.id = er."ruleId"
    WHERE er."createdAt" > '<T_START>'
    ORDER BY er."createdAt";
    ```

    Verify: 5 DigestItem rows — one each for T1 (Newsletters), T2 (Receipts), T3 (Deals), T5 (Urgent), T7 (Uncertain). T4 (Marketing) and T6 (2FA) and T8 (Greers List) have no DIGEST action and therefore no DigestItem.

    Query F — Two-minute SLA (CLASS-08):
    For each test email, compute delta = ExecutedRule.createdAt - Gmail Sent timestamp. ALL deltas must be < 120 seconds.

    Query G — Inbox state for Urgent/Uncertain (CLASS-04):
    Open Gmail. Verify T5 (Urgent) and T7 (Uncertain) emails are in the inbox AND have their respective labels. Verify T1-T4 (Newsletters, Receipts, Deals, Marketing) and T8 (Greers List) are archived (not in inbox) and have their labels. Paste a screenshot link or text confirmation in chat.
  </how-to-verify>
  <acceptance_criteria>
    - Query A: 8 rows. confidenceScore non-null for T1-T7. confidenceScore null for T8.
    - Query B: action sets exactly match D-05 for every test email.
    - Query C: economy log lines >= 7. No "falling back to default" warnings.
    - Query D: exactly 1 ScheduledAction row for T6 with type=DELETE and hours_until between 23.5 and 24.5.
    - Query E: 5 DigestItem rows for T1, T2, T3, T5, T7 (one each).
    - Query F: every delta < 120 seconds.
    - Query G: Urgent/Uncertain in inbox with labels; Newsletters/Receipts/Deals/Marketing/Greers List archived with labels.
    - Operator pastes all query outputs in chat for record.
  </acceptance_criteria>
  <resume-signal>Reply "verified" with all query outputs pasted, or itemize any failure (which test, which acceptance criterion).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 24-hour follow-up — verify 2FA email auto-deleted from Gmail (CLASS-05)</name>
  <read_first>
    - Output of Task 1 Query D (recorded scheduledAt and ExecutedRule.id for T6)
    - .planning/phases/03-classification-engine/03-CONTEXT.md (D-11 — auto-delete via delayInMinutes=1440 + BullMQ)
  </read_first>
  <what-built>
    Wait 24 hours after sending T6 (the 2FA test email). The BullMQ delayed-action worker should have processed the ScheduledAction row and deleted the email from Gmail.

    Steps:

    1. ~24 hours after sending T6, check Gmail's Trash for the 2FA email.
    2. Run psql query:
       ```sql
       SELECT id, type, "scheduledAt", status, "lastError"
       FROM "ScheduledAction"
       WHERE id = '<T6_scheduled_action_id>';
       ```
  </what-built>
  <how-to-verify>
    1. Gmail: T6 test email is no longer in inbox or any label folder; it is in Trash (or permanently deleted).
    2. ScheduledAction row status has progressed from PENDING to COMPLETED (or whatever the success enum is — verify against schema).
    3. ScheduledAction.lastError IS NULL.
  </how-to-verify>
  <acceptance_criteria>
    - T6 email present in Gmail Trash (or deleted entirely).
    - ScheduledAction row status indicates successful execution.
    - No error recorded in lastError.
  </acceptance_criteria>
  <resume-signal>Reply "deleted" with the ScheduledAction row state, or describe failure (e.g., "still PENDING", "lastError = ...").</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write phase verification report</name>
  <files>
    - .planning/phases/03-classification-engine/03-05-SUMMARY.md
  </files>
  <read_first>
    - All Task 1 query outputs (pasted in chat)
    - All Task 2 follow-up results
    - .planning/REQUIREMENTS.md (CLASS-01 through CLASS-08 — to confirm each is now ✅)
  </read_first>
  <action>
    Write `.planning/phases/03-classification-engine/03-05-SUMMARY.md` with the following structure:

    ```markdown
    # Phase 3 — Classification Engine — Verification Summary

    **Verified:** <date>
    **Operator:** Rebekah
    **Production deploy SHA:** <from 03-04-SUMMARY>

    ## Requirement Sign-Off

    | Requirement | Evidence | Status |
    |-------------|----------|--------|
    | CLASS-01 | Query A: 8 ExecutedRule rows, one rule_name each, no duplicates | ✅ |
    | CLASS-02 | Query C: N economy / M escalated calls; no fallback warnings | ✅ |
    | CLASS-03 | Query A: confidenceScore populated for AI tier (T1-T7); null for static (T8) | ✅ |
    | CLASS-04 | Query G: Urgent/Uncertain in inbox; rest archived | ✅ |
    | CLASS-05 | Task 2: 2FA email in Trash after 24h | ✅ |
    | CLASS-06 | T8 row in Query A has STATIC matchMetadata; no AI log line for T8 | ✅ |
    | CLASS-07 | All 8 new rules (systemType=null = user/explicit tier) match before conversation meta-rule | ✅ (note: deeper CLASS-07 — actual user-authored rules from Phase 5 UI — extends in Phase 5) |
    | CLASS-08 | Query F: every delta < 120s | ✅ |

    ## Test Email Trace

    | # | Sent (Gmail) | ExecutedRule.createdAt | Delta | Rule | Confidence | Tier | Actions |
    |---|--------------|------------------------|-------|------|-----------|------|---------|
    | T1 | ... | ... | ...s | Newsletters | 0.XX | economy | LABEL,ARCHIVE,DIGEST |
    | T2 | ... | ... | ...s | Receipts | 0.XX | economy | ... |
    | ... (one row per test email)

    ## Anomalies / Notes

    - <Any unexpected behavior, e.g., escalation rate higher than expected; one rule confidence at 0.65; etc.>

    ## Cost Snapshot

    - Anthropic credit balance at start of test: $X.XX
    - Anthropic credit balance after Task 1: $X.XX
    - Delta (8 emails): $X.XX
    - Projected monthly burn from this delta extrapolated to 35 emails/day: $X.XX
    - Projected vs RESEARCH estimate ($1.88/mo): match / over / under

    ## Phase 3 Complete
    All 8 CLASS requirements verified in production. Hand off to Phase 4 (Daily Digest).
    ```

    Fill every `<...>` placeholder with values captured from Task 1/2 outputs.

    Update `.planning/REQUIREMENTS.md`: change CLASS-01 through CLASS-08 from `[ ]` to `[x]` and update the Traceability table status from "Pending" to "Complete (<date>)".

    Update `.planning/STATE.md`: set Current Position to Phase 4, mark Phase 3 complete in Performance Metrics.

    Update `.planning/ROADMAP.md`: change `- [ ] **Phase 3: Classification Engine**` to `- [x] **Phase 3: Classification Engine** ... *(completed <date>)*`.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-classification-engine/03-05-SUMMARY.md && grep -F "## Phase 3 Complete" .planning/phases/03-classification-engine/03-05-SUMMARY.md && grep -E "^\- \[x\] \*\*Phase 3:" .planning/ROADMAP.md && grep -F "[x] **CLASS-01**" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 03-05-SUMMARY.md exists with all 8 CLASS rows checked off and evidence column populated.
    - REQUIREMENTS.md: CLASS-01 through CLASS-08 marked `[x]`; traceability table rows show "Complete (<date>)".
    - ROADMAP.md: Phase 3 marked complete with date.
    - STATE.md: Current Position advanced to Phase 4.
  </acceptance_criteria>
  <done>Phase 3 verification report committed; planning state updated; ready for Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test emails -> production system | Real emails sent to live Gmail; cost incurred |
| Operator -> production logs | SSH read-only; no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Information Disclosure | Test email content captured in production logs | accept | Test emails are operator-controlled fixtures with no sensitive content; logs are operator-only access. |
| T-03-21 | Denial of Service | Burst of 8 test emails in <10 min could exceed rate limits | mitigate | Rate-limit guard in webhook (RECON Decision 1) is already production-hardened; 1 email/minute spacing recommended in Task 1. |
| T-03-22 | Tampering | Operator misclassifies query results as "passing" | mitigate | Acceptance criteria require pasting raw query output in chat — third-party (Claude) reviews before allowing resume signal. |
| T-03-23 | Repudiation | No record that verification ran | mitigate | Task 3 produces 03-05-SUMMARY.md with all evidence inline. |

ASVS L1: 14.1 — verification artifacts archived in repo; reproducible queries documented.
</threat_model>

<verification>
After all 3 tasks:
- 03-05-SUMMARY.md exists and is complete.
- All 8 CLASS requirements marked [x] in REQUIREMENTS.md.
- ROADMAP.md and STATE.md updated.
- Production system observably operating in three-tier mode with no fallback warnings.
</verification>

<success_criteria>
- All 8 CLASS-XX requirements pass acceptance with quoted evidence.
- 2FA delete fires within 24h ± tolerance (Task 2).
- Cost snapshot recorded for ongoing monitoring.
- Planning artifacts (REQUIREMENTS, ROADMAP, STATE) reflect Phase 3 complete.
</success_criteria>

<output>
After completion, `.planning/phases/03-classification-engine/03-05-SUMMARY.md` is the final phase artifact and the entry point for Phase 4 planning.
</output>
